---
phase: 02-teamlead-skills
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/hooks/stop-guard.js
  - .claude/settings.json
  - scripts/test-stop-guard.sh
autonomous: true
requirements: [PIPE-04]

must_haves:
  truths:
    - "stop-guard.js exits 0 when stop_hook_active is true in the JSON payload"
    - "stop-guard.js exits 0 when stop_hook_active is false (normal stop)"
    - "stop-guard.js exits 0 on malformed stdin (never blocks a session)"
    - "Stop hook is registered in .claude/settings.json under hooks.Stop"
    - "bash scripts/test-stop-guard.sh runs and prints 'All tests passed.' without error"
  artifacts:
    - path: ".claude/hooks/stop-guard.js"
      provides: "Stop hook infinite-loop guard"
      contains: "stop_hook_active"
    - path: ".claude/settings.json"
      provides: "Stop hook registration"
      contains: "stop-guard.js"
    - path: "scripts/test-stop-guard.sh"
      provides: "Forced-exit test"
      contains: "All tests passed"
  key_links:
    - from: ".claude/settings.json hooks.Stop"
      to: ".claude/hooks/stop-guard.js"
      via: "command field with absolute node path"
      pattern: "stop-guard\\.js"
    - from: "scripts/test-stop-guard.sh"
      to: ".claude/hooks/stop-guard.js"
      via: "pipe JSON payload to node"
      pattern: "stop_hook_active.*true"
---

<objective>
Implement the Stop hook infinite-loop guard: a Node.js script that reads the Stop hook JSON payload, checks `stop_hook_active`, and exits 0 immediately when true. Register it in settings.json. Provide an automated test script that proves the forced-exit behavior.

Purpose: Without this guard, a future Stop hook that exits 2 (blocking stop) would create an infinite loop — Claude forces continuation, triggers Stop again, blocks again. This guard is the circuit breaker.

Output: `.claude/hooks/stop-guard.js`, `scripts/test-stop-guard.sh`, updated `.claude/settings.json`.
</objective>

<execution_context>
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-teamlead-skills/02-CONTEXT.md
@.planning/phases/02-teamlead-skills/02-RESEARCH.md
</context>

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From .claude/hooks/task-state-guard.js (Node.js hook boilerplate — replicate exactly):
- Shebang: #!/usr/bin/env node
- stdin read pattern: let input = ''; const stdinTimeout = setTimeout(() => process.exit(0), 3000); process.stdin.setEncoding('utf8'); process.stdin.on('data', chunk => { input += chunk; }); process.stdin.on('end', () => { clearTimeout(stdinTimeout); try { ... } catch (e) { process.exit(0); } });
- JSON field to read: data.stop_hook_active (boolean, top-level in Stop hook payload)
- Stop hook does NOT write to stdout — no hookSpecificOutput, no modifiedInput

From .claude/settings.json (registration pattern — copy SessionStart shape, no matcher):
- Hook registration shape: { "hooks": [{ "type": "command", "command": "...", "timeout": 5 }] }
- Absolute node path used in all existing hooks: /Users/tarasbannyi/.nvm/versions/node/v22.18.0/bin/node
- Stop entry goes inside top-level "hooks" object as a sibling of "SessionStart", "PreToolUse", "PostToolUse"
- Stop hooks do NOT support a "matcher" field
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create stop-guard.js Stop hook</name>
  <files>.claude/hooks/stop-guard.js</files>
  <read_first>
    - .claude/hooks/task-state-guard.js — copy the stdin boilerplate exactly (shebang, stdinTimeout, process.stdin.on chain, try/catch, process.exit(0) in catch)
    - .claude/settings.json — confirm absolute node path used in existing hooks (/Users/tarasbannyi/.nvm/versions/node/v22.18.0/bin/node)
    - .planning/phases/02-teamlead-skills/02-RESEARCH.md — stop-guard.js pattern in Code Examples section, anti-patterns (do NOT read stop_hook_active from process.env)
  </read_first>
  <action>
    Create .claude/hooks/stop-guard.js as a new file following the task-state-guard.js stdin boilerplate.

    Shebang: #!/usr/bin/env node

    Comment block: stop-guard.js — Stop hook. Prevents infinite loop: exits 0 immediately if stop_hook_active is true. Registered in .claude/settings.json hooks.Stop array.

    stdin read: copy the exact pattern from task-state-guard.js lines 19-27 (let input = '', stdinTimeout = setTimeout(() => process.exit(0), 3000), setEncoding utf8, on('data'), on('end')).

    Inside on('end') handler:
    - clearTimeout(stdinTimeout)
    - try block: const data = JSON.parse(input)
    - Check: if (data.stop_hook_active === true) { process.exit(0); }  — with comment "CRITICAL: if already in forced-continuation state, let Claude stop. This breaks the infinite loop."
    - Default (stop_hook_active is false or absent): process.exit(0) with comment "Allow stopping normally."
    - catch(e): process.exit(0) with comment "Silent fail — never block the session on a parse error."

    Key constraints (per D-11 and 02-RESEARCH.md Pitfall 1):
    - Read stop_hook_active from data (JSON stdin) — NOT from process.env.CLAUDE_STOP_HOOK_ACTIVE
    - Never write to stdout (no hookSpecificOutput, no JSON.stringify output) — Stop hook communicates only via exit code
    - Always exits 0 — Phase 2 does NOT need to block stopping (exit 2 is a future concern)
    - 3-second stdin timeout matches task-state-guard.js pattern exactly
  </action>
  <verify>
    <automated>grep -c "stop_hook_active" /Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/hooks/stop-guard.js</automated>
  </verify>
  <done>
    - .claude/hooks/stop-guard.js exists
    - File contains #!/usr/bin/env node shebang
    - File contains stop_hook_active check reading from JSON data (not process.env)
    - File contains stdinTimeout pattern matching task-state-guard.js
    - All code paths exit 0 (no exit 1 or exit 2)
    - echo '{"stop_hook_active":true}' | node .claude/hooks/stop-guard.js exits with code 0
    - echo '{"stop_hook_active":false}' | node .claude/hooks/stop-guard.js exits with code 0
    - echo 'broken json' | node .claude/hooks/stop-guard.js exits with code 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Register Stop hook in settings.json and create test script</name>
  <files>.claude/settings.json, scripts/test-stop-guard.sh</files>
  <read_first>
    - .claude/settings.json — read full file to understand current structure; the Stop entry goes inside the top-level "hooks" object as a sibling of "SessionStart", "PreToolUse", "PostToolUse"
    - .claude/hooks/task-state-guard.js — already read in Task 1; skip if in context
    - .planning/phases/02-teamlead-skills/02-PATTERNS.md — settings.json Stop hook registration pattern (lines showing the exact JSON shape and the node binary path)
    - .planning/phases/02-teamlead-skills/02-RESEARCH.md — test-stop-guard.sh script in Code Examples section
  </read_first>
  <action>
    PART A — Update .claude/settings.json:
    Add a "Stop" key to the top-level "hooks" object (sibling of "SessionStart", "PostToolUse", "PreToolUse"). The value is:
    [{ "hooks": [{ "type": "command", "command": "\"/Users/tarasbannyi/.nvm/versions/node/v22.18.0/bin/node\" \"$CLAUDE_PROJECT_DIR\"/.claude/hooks/stop-guard.js", "timeout": 5 }] }]

    Stop hooks do NOT support a "matcher" field — omit it. The timeout is 5 (matching existing hooks).
    Preserve all existing hooks content exactly — only add the "Stop" key.

    PART B — Create scripts/test-stop-guard.sh:
    First create the scripts/ directory (it does not exist).
    Write scripts/test-stop-guard.sh with:
    - Shebang: #!/usr/bin/env bash
    - Comment: test-stop-guard.sh — Forced-exit test for stop-guard.js. Asserts stop-guard.js exits 0 when stop_hook_active is true. Run: bash scripts/test-stop-guard.sh
    - set -e at the top
    - HOOK variable: HOOK="$(dirname "$0")/../.claude/hooks/stop-guard.js"
    - NODE variable: NODE="/Users/tarasbannyi/.nvm/versions/node/v22.18.0/bin/node"
    - Test 1: PAYLOAD with stop_hook_active:true — pipe to "$NODE" "$HOOK", then echo "PASS: stop_hook_active=true exits 0"
    - Test 2: PAYLOAD with stop_hook_active:false — pipe to "$NODE" "$HOOK", then echo "PASS: stop_hook_active=false exits 0"
    - Final: echo "All tests passed."

    Test 1 payload: {"session_id":"test","hook_event_name":"Stop","stop_hook_active":true,"cwd":".","transcript_path":"/tmp/test.jsonl"}
    Test 2 payload: {"session_id":"test","hook_event_name":"Stop","stop_hook_active":false,"cwd":".","transcript_path":"/tmp/test.jsonl"}

    Make the script executable: chmod +x scripts/test-stop-guard.sh
  </action>
  <verify>
    <automated>bash /Users/tarasbannyi/TestAI/ai-agent-microservices/scripts/test-stop-guard.sh</automated>
  </verify>
  <done>
    - .claude/settings.json contains a "Stop" key in the hooks object with stop-guard.js in the command
    - scripts/test-stop-guard.sh exists and is executable
    - bash scripts/test-stop-guard.sh completes with exit 0 and prints "All tests passed."
    - node -e "const s=require('./.claude/settings.json'); console.log(s.hooks.Stop ? 'OK' : 'MISSING')" prints "OK"
    - All pre-existing settings.json entries (SessionStart, PreToolUse, PostToolUse) are preserved unchanged
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook stdin | JSON payload from Claude Code runtime — trusted process boundary, but must handle malformed input |
| settings.json | Read by Claude Code at session start — tampering would affect all hook registrations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | stop-guard.js stdin read | mitigate | 3-second stdin timeout (setTimeout → process.exit(0)) prevents the hook from hanging indefinitely and blocking the session |
| T-02-02 | Tampering | stop-guard.js JSON parse | mitigate | Wrap entire logic in try/catch with process.exit(0) in catch — malformed JSON never blocks the session |
| T-02-03 | Elevation of Privilege | stop-guard.js exit code | accept | Hook only ever exits 0; it cannot be weaponized to force session continuation (exit 2) from external input in Phase 2 |
| T-02-04 | Tampering | settings.json Stop entry | accept | settings.json is local config under version control; unauthorized modification requires local filesystem write access (already privileged) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Phase 2 installs no packages; Node.js built-ins only — no supply-chain risk |
</threat_model>

<verification>
Run the full verification suite:
1. bash /Users/tarasbannyi/TestAI/ai-agent-microservices/scripts/test-stop-guard.sh — must print "All tests passed."
2. node -e "const s=require('./.claude/settings.json'); console.log(s.hooks.Stop ? 'REGISTERED' : 'MISSING')" — must print "REGISTERED"
3. echo '{}' | node .claude/hooks/stop-guard.js; echo "exit: $?" — must print "exit: 0"
</verification>

<success_criteria>
- stop-guard.js exists at .claude/hooks/stop-guard.js, reads stop_hook_active from JSON stdin, exits 0 in all cases
- .claude/settings.json has a "Stop" hook entry pointing to stop-guard.js with absolute node path and timeout 5
- scripts/test-stop-guard.sh exists, runs without error, prints "All tests passed."
- PIPE-04 requirement: verified by bash scripts/test-stop-guard.sh exit 0
</success_criteria>

<output>
Create `.planning/phases/02-teamlead-skills/02-01-SUMMARY.md` when done
</output>
