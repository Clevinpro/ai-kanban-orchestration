---
phase: 03-sub-agents
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/team-lead-check.md
  - .planning/task-schema.yaml
  - .claude/commands/team-lead/plan.md
autonomous: true
requirements: [AGENT-06]

must_haves:
  truths:
    - "team-lead-check agent exists with Glob first, Grep last in tools; no Bash"
    - "team-lead-check reads SPEC.md path from spec: frontmatter field with fallback to epic glob"
    - "team-lead-check appends ## TeamLead Check block with capitalized Status: APPROVED or Status: REJECTED"
    - "team-lead-check returns one-line receipt [team-lead-check] APPROVED or [team-lead-check] REJECTED: reason"
    - "task-schema.yaml has spec: field documented as optional string (populated by plan.md)"
    - "plan.md populates spec: field in each generated TASK-XXX.md with the SPEC.md path"
  artifacts:
    - path: ".claude/agents/team-lead-check.md"
      provides: "TeamLead final acceptance check agent"
      contains: "name: team-lead-check"
    - path: ".planning/task-schema.yaml"
      provides: "Schema with spec: field"
      contains: "spec:"
    - path: ".claude/commands/team-lead/plan.md"
      provides: "Updated task generation to include spec: field"
      contains: "spec:"
  key_links:
    - from: ".claude/agents/team-lead-check.md"
      to: ".planning/task-schema.yaml spec: field"
      via: "reads spec: from task frontmatter"
      pattern: "spec:"
    - from: ".claude/commands/team-lead/plan.md"
      to: "generated TASK-XXX.md"
      via: "Write tool after user confirms — injects spec: field"
      pattern: "spec:"
---

<objective>
Create the team-lead-check sub-agent definition, add the spec: field to task-schema.yaml, and update plan.md to populate spec: in each generated task file. The team-lead-check agent is the final pipeline stage: it reads the SPEC.md (located via the spec: field), verifies the implementation meets every acceptance criterion, appends a ## TeamLead Check block to the task file, and returns an APPROVED or REJECTED receipt. Without the spec: field in task files, the agent cannot locate the SPEC.md — this plan closes that gap.

Purpose: Completes the sub-agent library (AGENT-06) and closes the spec: field gap identified in 03-RESEARCH.md Open Question 1. Phase 4 pipeline integration depends on all six agents being fully defined.

Output: .claude/agents/team-lead-check.md, updated .planning/task-schema.yaml, updated .claude/commands/team-lead/plan.md
</objective>

<execution_context>
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sub-agents/03-CONTEXT.md
@.planning/phases/03-sub-agents/03-RESEARCH.md
@.planning/phases/03-sub-agents/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create team-lead-check agent definition</name>
  <files>.claude/agents/team-lead-check.md</files>

  <read_first>
    - .claude/agents/gsd-plan-checker.md — reference agent that shares the read-heavy + Write pattern (no Bash); use for frontmatter structure reference
    - .planning/phases/03-sub-agents/03-RESEARCH.md — "Receipt Format Specification" (exact team-lead-check receipt strings), "Task File Annotation — Hook Safety" section (capitalized Status: required in ## TeamLead Check block), "Common Pitfalls" section 5 (team-lead-check missing SPEC.md path — must handle spec: absent gracefully), "Open Questions" Q1 (spec: field not yet in task-schema.yaml — fallback logic required)
    - .planning/phases/03-sub-agents/03-PATTERNS.md — team-lead-check pattern section (frontmatter block, spec field lookup steps, SPEC.md fallback via epic field, TeamLead Check append format with capitalized Status:, receipt protocol)
    - .planning/task-schema.yaml — current schema fields; team-lead-check reads spec: and epic: from task frontmatter
    - .claude/hooks/task-state-guard.js — confirm capitalized Status: in body blocks is hook-safe
  </read_first>

  <action>
    Create .claude/agents/team-lead-check.md as a new file. The file must begin with --- on line 1.

    YAML frontmatter fields (per D-10 and bug #60237):
    - name: team-lead-check (must match exactly — subagent_type used by execute.md)
    - description: one-sentence: reads SPEC.md acceptance criteria and full task history, verifies implementation satisfies all ACs, appends TeamLead Check block to task file, returns one-line receipt
    - tools: Glob, Read, Write, Grep (Glob at position 1, Grep at last position — bug #60237; Write needed to append ## TeamLead Check block; NO Bash per D-10)
    - color: yellow
    - NO Bash in tools (team-lead-check is read + write only, no command execution)

    System prompt body must implement this logic in order:

    1. Input: agent receives a task file path. Read the task file.

    2. SPEC.md location (two-step with fallback per 03-RESEARCH.md Open Question 1 and Pitfall 5):
       Step A — extract the spec: field from the task frontmatter. If spec: is present and non-empty, read the SPEC.md at that path.
       Step B — if spec: is absent or empty, use the task's epic: frontmatter field to search for SPEC.md: use Glob to find .planning/work/<epic>/SPEC.md. If found, read it. If neither step finds SPEC.md, return immediately: [team-lead-check] REJECTED: spec field missing from task file

    3. Read the full task file body including all appended sections (## Code Review, ## QA Results) to understand what was implemented and what was reviewed/tested.

    4. Verify each acceptance criterion from the SPEC.md "## Acceptance Criteria" section against the implementation described in the task body. Check every AC — do not stop at the first passing AC.

    5. Task file annotation: append a ## TeamLead Check block to the task file body using the Edit tool.
       CRITICAL: use capitalized Status: (capital S) — NOT lowercase status: — to avoid triggering task-state-guard.js hook.
       APPROVED format: ## TeamLead Check, then Status: APPROVED, then brief note of which ACs were verified.
       REJECTED format: ## TeamLead Check, then Status: REJECTED, then "Reason:" with specific AC number and explanation of what was missing.

    6. Receipt protocol: final line of output must be exactly one of:
       [team-lead-check] APPROVED
       [team-lead-check] REJECTED: <AC-N not met>
       [team-lead-check] REJECTED: spec field missing from task file

    The orchestrator (execute.md Phase 4) reads the receipt to gate the final done transition. If APPROVED, the orchestrator writes the status: forTeamLeadCheck → done transition. If REJECTED, the orchestrator loops back to the Developer stage.
  </action>

  <verify>
    <automated>
      grep -E "^tools: Glob,.+,Grep$" .claude/agents/team-lead-check.md &&
      grep "^tools:" .claude/agents/team-lead-check.md | grep -qv "Bash" &&
      grep "^name: team-lead-check$" .claude/agents/team-lead-check.md &&
      grep "spec:" .claude/agents/team-lead-check.md &&
      grep "epic:" .claude/agents/team-lead-check.md &&
      grep "Status: APPROVED" .claude/agents/team-lead-check.md &&
      grep "Status: REJECTED" .claude/agents/team-lead-check.md &&
      grep "\[team-lead-check\] APPROVED" .claude/agents/team-lead-check.md &&
      grep "\[team-lead-check\] REJECTED" .claude/agents/team-lead-check.md &&
      head -1 .claude/agents/team-lead-check.md | grep -q "^---$" &&
      echo "team-lead-check.md: PASS"
    </automated>
  </verify>

  <acceptance_criteria>
    - head -1 .claude/agents/team-lead-check.md returns ---
    - grep "^name: team-lead-check$" .claude/agents/team-lead-check.md exits 0
    - grep -E "^tools: Glob,.+,Grep$" .claude/agents/team-lead-check.md exits 0 (Glob first, Grep last)
    - grep "^tools:" .claude/agents/team-lead-check.md | grep -v "Bash" exits 0 (Bash absent from tools line per D-10)
    - grep "^color: yellow$" .claude/agents/team-lead-check.md exits 0
    - grep "spec:" .claude/agents/team-lead-check.md exits 0 (reads spec: frontmatter field)
    - grep "epic:" .claude/agents/team-lead-check.md exits 0 (fallback uses epic: field)
    - grep "Status: APPROVED" .claude/agents/team-lead-check.md exits 0 (capitalized Status: in TeamLead Check block)
    - grep "Status: REJECTED" .claude/agents/team-lead-check.md exits 0
    - grep "\[team-lead-check\] APPROVED" .claude/agents/team-lead-check.md exits 0
    - grep "\[team-lead-check\] REJECTED" .claude/agents/team-lead-check.md exits 0
    - grep "spec field missing" .claude/agents/team-lead-check.md exits 0 (handles missing spec: gracefully)
  </acceptance_criteria>

  <done>team-lead-check.md exists with tools: Glob, Read, Write, Grep (Glob first, Grep last, no Bash), system prompt with two-step SPEC.md lookup (spec: field + epic: fallback + graceful rejection), hook-safe capitalized Status: in ## TeamLead Check block, and APPROVED/REJECTED receipt protocol.</done>
</task>

<task type="auto">
  <name>Task 2: Add spec: field to task-schema.yaml and plan.md</name>
  <files>.planning/task-schema.yaml, .claude/commands/team-lead/plan.md</files>

  <read_first>
    - .planning/task-schema.yaml — read current schema in full before editing; identify where to insert spec: field (after the existing fields, before lifecycle section)
    - .claude/commands/team-lead/plan.md — read the full current implementation to find the task frontmatter generation step (STEP 5 or wherever TASK-XXX.md fields are written); identify the exact block where frontmatter fields are listed
    - .planning/phases/03-sub-agents/03-RESEARCH.md — "Open Questions" Q1 (spec: field absent from task-schema.yaml; plan.md must populate it); "Common Pitfalls" Pitfall 5 (team-lead-check cannot find SPEC.md without spec: field)
    - .planning/phases/03-sub-agents/03-CONTEXT.md — specifics section: "team-lead-check reads the SPEC.md path from the task file frontmatter (a spec field that plan.md populates)"
  </read_first>

  <action>
    Make two targeted edits — do NOT rewrite either file from scratch.

    EDIT 1 — .planning/task-schema.yaml:
    Add a spec: field entry to the fields: section. Place it after the updated-at: field, before the lifecycle: section. The field definition:
    - type: string
    - required: false (it is optional — tasks generated without a SPEC.md path are still valid)
    - note: "Path to the SPEC.md file for this task's epic. Populated by /team-lead:plan. Used by team-lead-check agent to locate acceptance criteria."

    Use the Edit tool to insert the spec: block after the last existing field (updated-at:) without disturbing surrounding whitespace or the lifecycle: section header.

    EDIT 2 — .claude/commands/team-lead/plan.md:
    Find the step in plan.md where TASK-XXX.md frontmatter fields are enumerated (the section that lists id:, title:, status:, priority:, repo:, epic:, complexity:, created-at:, updated-at:). Add a spec: field to this list.

    The spec: value must be the path to the SPEC.md file being planned — this is the $ARGUMENTS value (the SPEC.md path passed to /team-lead:plan). When writing each TASK-XXX.md, include:
    spec: <path-to-SPEC.md>  (the same SPEC.md path used as input to this plan invocation)

    Use the Edit tool with a narrow old_string that uniquely matches the existing field list to insert the new spec: field without disrupting surrounding content. Do NOT rewrite the entire plan.md body.

    After both edits:
    - task-schema.yaml has spec: field documented in the fields: section
    - plan.md includes spec: in the generated task frontmatter with the SPEC.md path value
    - team-lead-check can now read spec: directly from task frontmatter (fallback to epic glob is still needed for tasks generated before this change)
  </action>

  <verify>
    <automated>
      grep -A3 "^  spec:" .planning/task-schema.yaml &&
      grep "spec:" .claude/commands/team-lead/plan.md &&
      echo "task-schema and plan.md spec: field: PASS"
    </automated>
  </verify>

  <acceptance_criteria>
    - grep "^  spec:" .planning/task-schema.yaml exits 0 (spec: field in schema)
    - grep "required: false" .planning/task-schema.yaml (after spec: block) exits 0 (spec is optional)
    - grep "team-lead-check\|SPEC.md\|spec field" .planning/task-schema.yaml exits 0 (note references team-lead-check usage)
    - grep "spec:" .claude/commands/team-lead/plan.md exits 0 (spec: in task frontmatter generation)
    - the lifecycle: section in task-schema.yaml is intact (Edit did not overwrite it)
    - the plan.md frontmatter of the SLASH COMMAND (name:, description:, allowed-tools:) is intact (Edit targeted only the task generation step, not command frontmatter)
  </acceptance_criteria>

  <done>task-schema.yaml documents spec: as an optional string field with a note. plan.md includes spec: <SPEC.md path> in each generated task's frontmatter. team-lead-check now has a populated spec: field to read from task files, with the epic: fallback glob as a safety net for legacy tasks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| team-lead-check → SPEC.md filesystem | Agent reads SPEC.md at path from spec: field; path is user-supplied via /team-lead:plan |
| team-lead-check annotation → task file body | Agent writes ## TeamLead Check block via Edit; hook validates for lowercase status: patterns |
| plan.md → generated task file spec: field | spec: value is the SPEC.md path from $ARGUMENTS; could be tampered if SPEC.md path contains special characters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | team-lead-check body annotation (hook evasion) | mitigate | System prompt mandates capitalized Status: (capital S) in ## TeamLead Check block; task-state-guard.js hook regex is case-sensitive lowercase-only; verified by grep "Status: APPROVED" acceptance criterion |
| T-03-03-02 | Spoofing | spec: field path injection | mitigate | team-lead-check reads spec: from task frontmatter (written by plan.md from $ARGUMENTS); no shell expansion; read-only Glob lookup |
| T-03-03-03 | Repudiation | team-lead-check missing SPEC.md | mitigate | Explicit rejection receipt [team-lead-check] REJECTED: spec field missing from task file — orchestrator surfaces this to user rather than silently failing |
| T-03-03-04 | Tampering | spec: field absent in old tasks | mitigate | team-lead-check system prompt includes fallback: if spec: absent, glob .planning/work/<epic>/SPEC.md using epic: field value |
| T-03-03-05 | Tampering | plan.md schema extension — overwrite risk | mitigate | Both task-schema.yaml and plan.md edits use narrow Edit (old_string) rather than full file rewrites; lifecycle: section and command frontmatter preserved per acceptance criteria |
</threat_model>

<verification>
Run after all tasks complete:

```bash
# team-lead-check exists and has correct frontmatter
grep "^name: team-lead-check$" .claude/agents/team-lead-check.md
grep -E "^tools: Glob,.+,Grep$" .claude/agents/team-lead-check.md
grep "^tools:" .claude/agents/team-lead-check.md | grep -v "Bash"  # Bash must be absent

# spec: field in schema and plan.md
grep "^  spec:" .planning/task-schema.yaml
grep "spec:" .claude/commands/team-lead/plan.md

# All six agents now exist
ls .claude/agents/be-developer.md .claude/agents/fe-developer.md .claude/agents/code-reviewer.md .claude/agents/qa-be.md .claude/agents/qa-fe.md .claude/agents/team-lead-check.md

# All six have name: fields
grep -E "^name:" .claude/agents/be-developer.md .claude/agents/fe-developer.md .claude/agents/code-reviewer.md .claude/agents/qa-be.md .claude/agents/qa-fe.md .claude/agents/team-lead-check.md
```
</verification>

<success_criteria>
- team-lead-check.md exists with tools: Glob, Read, Write, Grep (Glob first, Grep last, no Bash), SPEC.md two-step lookup (spec: field + epic: fallback), hook-safe Status: annotation, APPROVED/REJECTED receipts
- task-schema.yaml has spec: field as optional string with note referencing team-lead-check
- plan.md generates spec: field in each task using the SPEC.md path from $ARGUMENTS
- All six agent files (.claude/agents/be-developer.md, fe-developer.md, code-reviewer.md, qa-be.md, qa-fe.md, team-lead-check.md) exist and pass structural validation
</success_criteria>

<output>
Create .planning/phases/03-sub-agents/03-03-SUMMARY.md when done.
</output>
