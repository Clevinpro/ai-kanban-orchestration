---
phase: 06-kanban-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - kanban-server/client/src/App.jsx
  - kanban-server/client/src/components/Board.jsx
  - kanban-server/client/src/components/TaskCard.jsx
autonomous: false
requirements:
  - KANBAN-04
must_haves:
  truths:
    - "Dragging a task card from one column and dropping it on another column moves the card immediately (optimistic update)"
    - "After a successful PATCH, the task file's frontmatter status field is updated to the new status value"
    - "After a failed PATCH (non-2xx or network error), the card reverts to its original column"
    - "Dragging a card and dropping it in the same column is a no-op (no PATCH fired, no state change)"
    - "All six columns accept card drops (readyForDevelop, inProgress, inReview, inTesting, forTeamLeadCheck, done)"
    - "Cards can be dragged by clicking and holding anywhere on the card surface"
  artifacts:
    - path: "kanban-server/client/src/App.jsx"
      provides: "dispatch passed to Board; DRAG_OPTIMISTIC and DRAG_REVERT actions active in reducer"
      contains: "DRAG_OPTIMISTIC"
    - path: "kanban-server/client/src/components/Board.jsx"
      provides: "DragDropContext wrapping all columns, Droppable per column, handleDragEnd with PATCH call"
      contains: "DragDropContext"
    - path: "kanban-server/client/src/components/TaskCard.jsx"
      provides: "Draggable wrapping each card with provided.innerRef and drag handle props"
      contains: "Draggable"
  key_links:
    - from: "Board.jsx handleDragEnd"
      to: "PATCH /tasks/:id/status"
      via: "fetch('/tasks/' + draggableId + '/status', { method: 'PATCH' })"
      pattern: "method.*PATCH"
    - from: "Board.jsx handleDragEnd on failure"
      to: "App.jsx boardReducer DRAG_REVERT"
      via: "dispatch({ type: 'DRAG_REVERT', taskId, originalStatus })"
      pattern: "DRAG_REVERT"
    - from: "Board.jsx DragDropContext onDragEnd"
      to: "App.jsx boardReducer DRAG_OPTIMISTIC"
      via: "dispatch({ type: 'DRAG_OPTIMISTIC', taskId: draggableId, newStatus })"
      pattern: "DRAG_OPTIMISTIC"
---

<objective>
Wire @hello-pangea/dnd drag-and-drop into the existing Board and TaskCard components. This plan completes KANBAN-04: dragging a card to a different column writes the updated status to the task file's YAML frontmatter via PATCH /tasks/:id/status (added in Plan 06-01), with optimistic local state update and revert on failure.

Three files are modified (not created fresh):
- App.jsx: pass dispatch to Board; confirm DRAG_OPTIMISTIC and DRAG_REVERT reducer cases are wired (they were declared as no-ops in Plan 06-02 — activate them here if needed).
- Board.jsx: wrap with DragDropContext, wrap each column with Droppable, implement handleDragEnd.
- TaskCard.jsx: wrap card content with Draggable, using React.forwardRef already established in Plan 06-02.

Purpose: Delivers the final success criterion SC-4 (drag card between columns writes status to task file frontmatter).

Output: Modified Board.jsx, TaskCard.jsx, App.jsx. The board remains live-updating via SSE while also accepting drag-and-drop writes.
</objective>

<execution_context>
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/ROADMAP.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/STATE.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/phases/06-kanban-ui/06-CONTEXT.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/phases/06-kanban-ui/06-RESEARCH.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/phases/06-kanban-ui/06-PATTERNS.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/phases/06-kanban-ui/06-02-SUMMARY.md

<interfaces>
<!-- @hello-pangea/dnd API (from RESEARCH.md Pattern 3) -->

DragDropContext — wraps the entire board, receives onDragEnd callback:
  import { DragDropContext } from '@hello-pangea/dnd';
  <DragDropContext onDragEnd={handleDragEnd}>

Droppable — wraps each column div, droppableId MUST equal the status string (e.g. 'inProgress'):
  import { Droppable } from '@hello-pangea/dnd';
  <Droppable droppableId={status}>
    {(provided) => (
      <div ref={provided.innerRef} {...provided.droppableProps}>
        {/* cards */}
        {provided.placeholder}  // REQUIRED — prevents column height collapse (RESEARCH.md Pitfall 3)
      </div>
    )}
  </Droppable>

Draggable — wraps each card, draggableId MUST equal task.id, index = position in column:
  import { Draggable } from '@hello-pangea/dnd';
  <Draggable draggableId={task.id} index={index}>
    {(provided) => (
      <div ref={provided.innerRef} {...provided.draggableProps} {...provided.dragHandleProps}>
        <TaskCard task={task} />
      </div>
    )}
  </Draggable>

onDragEnd result shape:
  {
    draggableId: "TASK-001",     // task.id
    source: { droppableId: "inProgress", index: 0 },
    destination: { droppableId: "inReview", index: 0 } | null,  // null if dropped outside
    reason: "DROP" | "CANCEL"
  }

No-op conditions (do NOT dispatch or PATCH):
  - !result.destination
  - result.reason === 'CANCEL'
  - result.destination.droppableId === result.source.droppableId

handleDragEnd flow (D-08 optimistic strategy):
  1. Validate no-op conditions — return early
  2. dispatch({ type: 'DRAG_OPTIMISTIC', taskId: draggableId, newStatus: destination.droppableId })
  3. fetch('/tasks/' + draggableId + '/status', { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ status: destination.droppableId }) })
     .then(res => { if (!res.ok) dispatch({ type: 'DRAG_REVERT', taskId: draggableId, originalStatus: source.droppableId }); })
     .catch(() => dispatch({ type: 'DRAG_REVERT', taskId: draggableId, originalStatus: source.droppableId }));

<!-- TaskCard.jsx: React.forwardRef already set up in Plan 06-02 -->
<!-- If Plan 06-02 did NOT implement forwardRef, add it here: -->
const TaskCard = React.forwardRef(({ task }, ref) => (
  <div ref={ref} className="bg-white rounded-md shadow-sm p-2 mb-1.5 text-xs cursor-grab" ...>
    ...card content...
  </div>
));

<!-- NOTE: Draggable's provided.innerRef passes a callback ref — it is passed as the ref prop -->
<!-- to the forwardRef component or directly to the div. Either approach works. -->
<!-- Simplest: wrap the TaskCard's outer div with Draggable inside Board.jsx/Column, NOT inside -->
<!-- TaskCard itself. This keeps TaskCard a pure display component (no dnd imports needed). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DragDropContext + Droppable to Board.jsx, Draggable to TaskCard.jsx, activate reducer actions in App.jsx</name>
  <files>
    kanban-server/client/src/App.jsx,
    kanban-server/client/src/components/Board.jsx,
    kanban-server/client/src/components/TaskCard.jsx
  </files>
  <read_first>
    - kanban-server/client/src/App.jsx (read the current file — confirm dispatch is being passed to Board as prop; confirm DRAG_OPTIMISTIC and DRAG_REVERT switch cases exist in boardReducer)
    - kanban-server/client/src/components/Board.jsx (read the current file — understand the current column rendering structure before adding Droppable wrappers)
    - kanban-server/client/src/components/TaskCard.jsx (read the current file — confirm whether React.forwardRef was implemented in Plan 06-02 or needs adding here)
    - .planning/phases/06-kanban-ui/06-RESEARCH.md (Pattern 3 DragDropContext + Droppable + Draggable patterns; Pitfall 3 provided.placeholder; Pitfall 4 SSE race)
    - .planning/phases/06-kanban-ui/06-PATTERNS.md (Board.jsx section lines 293-334; TaskCard.jsx Draggable pattern lines 414-436)
    - .planning/phases/06-kanban-ui/06-CONTEXT.md (D-07 @hello-pangea/dnd locked; D-08 optimistic strategy; D-09 six valid drag targets)
    - .planning/phases/06-kanban-ui/06-02-SUMMARY.md (confirm what Plan 06-02 produced — especially TaskCard forwardRef status and reducer action status)
  </read_first>
  <action>
Make three coordinated edits. Read all three files first to understand current state before touching anything.

EDIT 1 — App.jsx:
Confirm that `dispatch` is passed as a prop to Board (`<Board tasks={state} dispatch={dispatch} />`). If it is not, add dispatch to the Board props.
Confirm that the boardReducer includes DRAG_OPTIMISTIC and DRAG_REVERT cases. If Plan 06-02 added them as no-ops (returning state unchanged), replace them with the full implementations from RESEARCH.md Pattern 4:
- DRAG_OPTIMISTIC: find the task in any column (iterate COLUMN_ORDER), remove it from all columns, add it to the newStatus column with status overridden to newStatus.
- DRAG_REVERT: delegate to the SSE_UPDATE case handler passing { id: taskId, status: originalStatus } — same pattern as RESEARCH.md Pattern 4 lines 425-430.
If Plan 06-02 already implemented these correctly, make no change to App.jsx.

EDIT 2 — Board.jsx:
Add import at top: `import { DragDropContext, Droppable } from '@hello-pangea/dnd';`

Wrap the outer container div with `<DragDropContext onDragEnd={handleDragEnd}>`. The existing flex container (the six-column grid div with classes `flex gap-2 p-3 min-h-screen bg-gray-50`) becomes the direct child of DragDropContext.

Add handleDragEnd function inside the Board component (before the return statement):
```
function handleDragEnd(result) {
  const { draggableId, source, destination, reason } = result;
  if (!destination || reason === 'CANCEL') return;
  if (destination.droppableId === source.droppableId) return;
  const newStatus = destination.droppableId;
  const originalStatus = source.droppableId;
  dispatch({ type: 'DRAG_OPTIMISTIC', taskId: draggableId, newStatus });
  fetch('/tasks/' + draggableId + '/status', {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ status: newStatus }),
  }).then(function(res) {
    if (!res.ok) dispatch({ type: 'DRAG_REVERT', taskId: draggableId, originalStatus });
  }).catch(function() {
    dispatch({ type: 'DRAG_REVERT', taskId: draggableId, originalStatus });
  });
}
```

For each column in the COLUMN_ORDER.map, wrap the column content div with Droppable:
- droppableId={status} — the droppableId MUST equal the status string value (e.g. 'inProgress') per D-09
- Use the render prop pattern: `{(provided) => <div ref={provided.innerRef} {...provided.droppableProps} className="...">{...cards...}{provided.placeholder}</div>}`
- The column header h2 stays inside the Droppable div (above the task list)
- `{provided.placeholder}` MUST be the last child inside the Droppable div (RESEARCH.md Pitfall 3 — omitting causes column height collapse and incorrect drop positions)

For each task in tasks[status], wrap with Draggable:
- `<Draggable draggableId={task.id} index={index} key={task.id}>`
- Use render prop: `{(provided) => <div ref={provided.innerRef} {...provided.draggableProps} {...provided.dragHandleProps}><TaskCard task={task} /></div>}`
- The wrapper div for Draggable uses `provided.draggableProps` AND `provided.dragHandleProps` — both must be spread (entire card is the drag handle, not a separate grip element)

EDIT 3 — TaskCard.jsx:
If Plan 06-02 implemented `React.forwardRef` on TaskCard: TaskCard no longer needs to be a forwardRef component because the Draggable wrapper div (in Board.jsx) handles `provided.innerRef` directly. The TaskCard component can remain a plain component receiving only `{ task }` as props.

If Plan 06-02 did NOT implement React.forwardRef: TaskCard stays as a plain component — no change needed.

In either case: verify TaskCard's outer div does NOT have the cursor-grab class duplicated from both the Draggable wrapper div (in Board) and itself. Remove cursor-grab from TaskCard's outer div if the Draggable wrapper in Board.jsx already provides it. Add `cursor-grab` to the Draggable wrapper div in Board.jsx to maintain D-11 visual density.

Do NOT change any of TaskCard's display logic — title, repo badge, epic, complexity rendering is complete from Plan 06-02.
  </action>
  <verify>
    <automated>
      cd /Users/tarasbannyi/TestAI/ai-agent-microservices && grep -c 'DragDropContext' kanban-server/client/src/components/Board.jsx && grep -c 'Droppable' kanban-server/client/src/components/Board.jsx && grep -c 'provided.placeholder' kanban-server/client/src/components/Board.jsx && grep -c 'Draggable' kanban-server/client/src/components/Board.jsx && grep -c 'DRAG_OPTIMISTIC' kanban-server/client/src/App.jsx && grep -c "method.*PATCH" kanban-server/client/src/components/Board.jsx && cd kanban-server/client && npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - kanban-server/client/src/components/Board.jsx contains `DragDropContext` (import + usage, grep -c returns at least 2)
    - kanban-server/client/src/components/Board.jsx contains `Droppable` (import + usage, grep -c returns at least 2)
    - kanban-server/client/src/components/Board.jsx contains `provided.placeholder` (grep -c returns 1 — exactly once, at the end of the Droppable render prop)
    - kanban-server/client/src/components/Board.jsx contains `Draggable` (import + usage, grep -c returns at least 2)
    - kanban-server/client/src/components/Board.jsx contains `method` and `PATCH` in handleDragEnd (grep -c "method.*PATCH" or "PATCH" returns at least 1)
    - kanban-server/client/src/components/Board.jsx contains `DRAG_REVERT` (grep -c returns 1)
    - kanban-server/client/src/App.jsx contains `DRAG_OPTIMISTIC` in the reducer with implementation (not just a no-op, grep -c returns at least 1)
    - kanban-server/client/src/App.jsx passes `dispatch` to Board component (grep -c "dispatch={dispatch}" returns 1)
    - `cd kanban-server/client && npm run build` exits 0 (no TypeScript or import errors)
  </acceptance_criteria>
  <done>Board.jsx has DragDropContext wrapping all columns, Droppable per column with placeholder, Draggable per card; App.jsx passes dispatch to Board and has active DRAG_OPTIMISTIC/DRAG_REVERT cases; npm run build exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end drag-and-drop verification</name>
  <what-built>
    Full drag-and-drop integration: @hello-pangea/dnd wrapping all six columns and all task cards. Dragging a card to a new column fires PATCH /tasks/:id/status which writes the new status to the task file's YAML frontmatter. Optimistic update shows card in new column immediately; failure reverts card to original column.
  </what-built>
  <how-to-verify>
    Prerequisites: kanban-server running (`node kanban-server/index.js`), Vite dev server running (`cd kanban-server && npm run dev`), board visible at http://localhost:5173.

    Test 1 — Basic drag-and-drop:
    1. Note the current column of any visible task card
    2. Click and hold on the card, drag it to a different column, release
    3. Verify: card moves to the new column immediately (optimistic update)
    4. Verify: the task file under .planning/work/ now shows the new status value in its frontmatter (`grep "^status:" .planning/work/**/*.md`)

    Test 2 — SSE confirmation:
    5. After step 3, verify the card stays in the new column (SSE event from the server write confirms the move rather than reverting it)

    Test 3 — Same column drop (no-op):
    6. Drag a card and drop it in the same column
    7. Verify: card position unchanged, no PATCH request fired (check browser Network tab — no PATCH request should appear)

    Test 4 — All columns accept drops:
    8. Verify at least one successful drag into each of the six columns (or verify all column headers have the droppable=true attribute in the DOM)

    Test 5 — Build output works in production mode:
    9. `cd kanban-server/client && npm run build` — exits 0
    10. `node kanban-server/index.js` (restart if running) and open http://localhost:6111 — board visible with same functionality

    SC-4 final check (ROADMAP Phase 6 success criteria):
    "Dragging a card to a different column writes the updated status to the task file's YAML frontmatter"
    Verify by grepping the task file after a drag: `grep "^status:" .planning/work/<epic>/<TASK-ID>.md` shows the new status value.
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass (drag works, file updated, same-column no-op confirmed). Describe any failures — include which test step failed and what the actual behavior was.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser handleDragEnd → PATCH /tasks/:id/status | newStatus from destination.droppableId is a client-side value derived from COLUMN_ORDER; validated again by the server's VALID_STATUSES check in index.js |
| PATCH response → DRAG_REVERT dispatch | Non-2xx response triggers client-side state revert; no server data consumed on error path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | handleDragEnd newStatus value | mitigate | droppableId values are hard-coded in COLUMN_ORDER — only six valid status strings can be produced as drop targets (D-09); server also validates via VALID_STATUSES Set (defense in depth) |
| T-06-08 | Tampering | PATCH request with invalid taskId | mitigate | task.id originates from SSE payload which originates from parseTaskFile() — all task IDs match TASK-\\d{3} format; server validates with regex before any file I/O (T-06-01 from Plan 06-01) |
| T-06-09 | Denial of Service | Rapid drag events firing multiple PATCH requests | accept | Local dev tool — single user; @hello-pangea/dnd onDragEnd fires once per completed drag, not continuously |
| T-06-SC | Tampering | npm install for @hello-pangea/dnd | mitigate | Already installed in Plan 06-02 (checkpoint:human-verify Task 1 approved); no new installs in this plan |
</threat_model>

<verification>
After Task 1 and before Task 2 (automated checks):

1. `grep -c 'DragDropContext' kanban-server/client/src/components/Board.jsx` — returns 2 (import + JSX usage)
2. `grep -c 'provided.placeholder' kanban-server/client/src/components/Board.jsx` — returns 1 (exactly one placeholder)
3. `grep -c 'DRAG_OPTIMISTIC' kanban-server/client/src/App.jsx` — returns at least 1
4. `grep -c "dispatch={dispatch}" kanban-server/client/src/App.jsx` — returns 1
5. `cd kanban-server/client && npm run build` — exits 0, no errors

After Task 2 (human-verify):
- SC-4 confirmed: drag card → task file frontmatter status updated on disk
- Same-column drop is a no-op (no PATCH fired)
- Production build serves board at http://localhost:6111
</verification>

<success_criteria>
- Board.jsx uses DragDropContext wrapping all six Droppable columns
- Each Droppable column has provided.placeholder as last child (Pitfall 3 prevention)
- Each task card is wrapped with Draggable in Board.jsx (draggableId=task.id, index=position)
- handleDragEnd in Board.jsx fires DRAG_OPTIMISTIC dispatch then PATCH /tasks/:id/status
- PATCH failure dispatches DRAG_REVERT, reverting the card to its original column
- App.jsx reducer DRAG_OPTIMISTIC and DRAG_REVERT cases have full implementations (not no-ops)
- npm run build exits 0 after all three file edits
- Human-verified: drag-and-drop writes status to task file frontmatter (SC-4)
</success_criteria>

<output>
Create `.planning/phases/06-kanban-ui/06-03-SUMMARY.md` when done.
</output>
