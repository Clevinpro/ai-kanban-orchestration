---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .claude/commands/team-lead/plan.md
  - .claude/commands/team-lead/execute.md
autonomous: true
requirements:
  - FOUND-03
  - FOUND-06
  - FOUND-07

must_haves:
  truths:
    - "The root CLAUDE.md contains hard routing rules only — no NestJS, React, Kafka, Prisma, or GSD workflow content"
    - "The root CLAUDE.md explicitly references ai-platform/CLAUDE.md, ai-platform-fe/CLAUDE.md, .planning/work/<epic>/, /team-lead:plan, and /team-lead:execute"
    - "The root CLAUDE.md is under 200 lines"
    - "/team-lead:plan stub command exists and acknowledges the ~10 min max task size constraint"
    - "/team-lead:execute stub command exists and acknowledges the fresh context window constraint"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Project constitution — routing rules only"
      contains: "ai-platform/CLAUDE.md"
    - path: ".claude/commands/team-lead/plan.md"
      provides: "Stub slash command /team-lead:plan with FOUND-06/07 constraints"
      contains: "10 minutes"
    - path: ".claude/commands/team-lead/execute.md"
      provides: "Stub slash command /team-lead:execute with FOUND-06/07 constraints"
      contains: "fresh context"
  key_links:
    - from: "CLAUDE.md"
      to: "ai-platform/CLAUDE.md"
      via: "explicit reference in Repos section"
      pattern: "ai-platform/CLAUDE\\.md"
    - from: "CLAUDE.md"
      to: "ai-platform-fe/CLAUDE.md"
      via: "explicit reference in Repos section"
      pattern: "ai-platform-fe/CLAUDE\\.md"
    - from: "CLAUDE.md"
      to: ".claude/commands/team-lead/"
      via: "Agent Workflow Entry Points section"
      pattern: "/team-lead:plan"
---

<objective>
Deliver the root project constitution (CLAUDE.md rewrite) and TeamLead stub slash commands that acknowledge the critical execution constraints.

Purpose: The root CLAUDE.md is the first file Claude Code loads in this workspace. It must route agents to the correct sub-repo context and surface the TeamLead entry points. The stub slash commands must declare the ~10 min and fresh-context constraints so that Phase 2 (which fills in the logic) inherits the right contract.

Output: Rewritten `CLAUDE.md` (routing rules only, under 200 lines), `.claude/commands/team-lead/plan.md` stub, `.claude/commands/team-lead/execute.md` stub.

After this plan, the workspace routing is correctly configured and the TeamLead entry points are visible as slash commands.
</objective>

<execution_context>
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md

<interfaces>
<!-- Key patterns and constraints. Extracted from codebase. -->

From ai-platform/CLAUDE.md (density/format pattern — exact style to replicate):
- First heading `#` is repo/project name only
- One-line context sentence after heading (no paragraph)
- All sections use `##` — no `###` in CLAUDE.md files
- Rules stated as bullet points with **bold** key term
- No JSDoc, no prose explanations — terse imperative statements only
- All content in English

From .claude/commands/gsd/plan-phase.md (slash command YAML frontmatter pattern):
- name: <namespace>:<command>  (folder:filename)
- description: one-line purpose
- argument-hint: "<args>"
- allowed-tools: list

Locked decisions from CONTEXT.md (D-01 to D-04):
- D-01: Rewrite from scratch — replace ALL existing content
- D-02: Hard routing rules only — NO tech stack, NO NestJS/React patterns, NO GSD workflow content
- D-03: Do NOT include GSD workflow enforcement section
- D-04: Must reference (1) ai-platform/CLAUDE.md, (2) ai-platform-fe/CLAUDE.md, (3) .planning/work/<epic>/, (4) /team-lead:plan and /team-lead:execute
- FOUND-03: Under 200 lines hard limit
- FOUND-06: Max ~10 minutes per task — must be visible in TeamLead stubs
- FOUND-07: Fresh context window per task — must be visible in TeamLead stubs
</interfaces>
</context>

## Phase Goal

**As a** developer, **I want to** open this workspace and immediately understand which sub-repo to work in and how to invoke the agent pipeline, **so that** I route myself and any agent to the correct context without reading through hundreds of lines of boilerplate.

<tasks>

<task type="auto">
  <name>Task 1: Rewrite root CLAUDE.md as project routing constitution</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md — current file (read fully before rewriting; the current content is a GSD workflow guide that must be entirely replaced per D-01)
    - ai-platform/CLAUDE.md — density and format reference: heading style, section headers, bullet-point rules, terse imperative statements. Replicate this density, not the content.
    - .planning/phases/01-foundation/01-CONTEXT.md — D-01 through D-04 (exact content requirements), D-17/D-18 (task ID format and file location to reference)
    - .planning/phases/01-foundation/01-RESEARCH.md — "Root CLAUDE.md Structure (Reference Template)" code example and "Pitfall 3: Root CLAUDE.md Line Count Exceeds 200"
    - .planning/phases/01-foundation/01-PATTERNS.md — Root CLAUDE.md pattern assignment (what to include and explicitly exclude)
  </read_first>
  <action>
Rewrite `CLAUDE.md` from scratch. Replace ALL existing content (the current file is a GSD workflow guide — delete it entirely and replace with the routing constitution).

The rewritten file must contain exactly these sections (per D-04):

`# AI Agent Dev Workflow` — project name header (one line only, no subtitle)

`## Repos` — references to both sub-repos:
- `ai-platform/` entry: NestJS backend (api-gateway, auth-service, ai-service); Agent context file: `ai-platform/CLAUDE.md`; Conventions skill: `ai-platform/.claude/skills/be-conventions/SKILL.md`
- `ai-platform-fe/` entry: React frontend (shell, auth, chat MFEs); Agent context file: `ai-platform-fe/CLAUDE.md`; Conventions skill: `ai-platform-fe/.claude/skills/fe-conventions/SKILL.md`

`## Task Files` — task file location and format rules:
- Task files live at `.planning/work/<epic-name>/TASK-XXX.md`
- Task ID format: `TASK-001` (three-digit zero-padded per D-17)
- The epic field in frontmatter must match the parent directory name (per D-18)

`## Agent Workflow Entry Points` — the two commands:
- `/team-lead:plan <SPEC.md>` — Break a spec into TASK-XXX.md files for human review
- `/team-lead:execute <TASK-ID>` — Run the full automated pipeline for one task

`## Routing Rules` — imperative directives (bullet points):
- Before editing ai-platform/ code: load `ai-platform/CLAUDE.md`
- Before editing ai-platform-fe/ code: load `ai-platform-fe/CLAUDE.md`
- Never edit across both repos in a single task (tasks are `repo: be` or `repo: fe`, never `repo: both`)
- Each task executes in a fresh context window — do not assume state from previous tasks

Hard constraints (check before writing final file):
- File must be UNDER 200 LINES total (count before saving)
- Must NOT contain any of: NestJS, React, Kafka, Prisma, GSD workflow instructions, technology stack details
- Must NOT include a GSD workflow enforcement section (per D-03)
- Use the same format density as ai-platform/CLAUDE.md: `##` headers, bullet points with `**bold**` key terms, no prose paragraphs
  </action>
  <verify>
    <automated>wc -l /Users/tarasbannyi/TestAI/ai-agent-microservices/CLAUDE.md && grep -c "ai-platform/CLAUDE.md" /Users/tarasbannyi/TestAI/ai-agent-microservices/CLAUDE.md && grep -c "team-lead:plan" /Users/tarasbannyi/TestAI/ai-agent-microservices/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md line count is under 200 (confirmed by wc -l output)
    - CLAUDE.md contains "ai-platform/CLAUDE.md" (explicit reference to BE sub-repo context)
    - CLAUDE.md contains "ai-platform-fe/CLAUDE.md" (explicit reference to FE sub-repo context)
    - CLAUDE.md contains ".planning/work/" (task file root reference)
    - CLAUDE.md contains "TASK-" and three-digit format reference
    - CLAUDE.md contains "/team-lead:plan" (entry point reference)
    - CLAUDE.md contains "/team-lead:execute" (entry point reference)
    - CLAUDE.md does NOT contain "NestJS" or "React" or "Kafka" or "Prisma" (no tech stack content)
    - CLAUDE.md does NOT contain "gsd" or "GSD" (no GSD workflow enforcement per D-03)
  </acceptance_criteria>
  <done>Root CLAUDE.md is a pure routing constitution under 200 lines containing all four D-04 references and no tech stack or GSD content</done>
</task>

<task type="auto">
  <name>Task 2: Create TeamLead stub slash commands with constraint declarations</name>
  <files>.claude/commands/team-lead/plan.md, .claude/commands/team-lead/execute.md</files>
  <read_first>
    - .claude/commands/gsd/plan-phase.md — YAML frontmatter pattern (lines 1-16): name, description, argument-hint, allowed-tools fields. Copy exact structure, adapt content.
    - .planning/phases/01-foundation/01-RESEARCH.md — "TeamLead Stub Slash Command" code example (frontmatter + constraints body), Pattern 1 (slash command naming convention folder:file)
    - .planning/phases/01-foundation/01-PATTERNS.md — plan.md and execute.md pattern assignments, slash command YAML frontmatter shared pattern
    - .planning/phases/01-foundation/01-CONTEXT.md — FOUND-06 (~10 min max), FOUND-07 (fresh context window), D-15 (no repo: both)
  </read_first>
  <action>
Create `.claude/commands/team-lead/plan.md` (creates the `/team-lead:plan` slash command):

YAML frontmatter:
- `name: team-lead:plan`
- `description: Break a SPEC.md epic into TASK-XXX.md files with complexity scores. Pauses for human review before any code executes.`
- `argument-hint: "<path-to-SPEC.md>"`
- `allowed-tools:` Read, Write, Bash, Glob, Grep

Body must include a `## Constraints (MUST READ BEFORE PLANNING)` section with these exact constraints (per FOUND-06 and FOUND-07):
- `**Max task size: ~10 minutes of execution time.**` — if a task would take longer, split it. This is enforced by design, not by a timer.
- `**Fresh context window per task.**` — each task executes in isolation; no state from previous tasks is available. Design tasks to be fully self-contained.
- `**Repo isolation.**` — set `repo: be` or `repo: fe` per task. Never `repo: both` — split full-stack work into two separate tasks.
- `**Status on creation: readyForDevelop.**` — the status guard hook will reject any other initial status.

Body must include a `## Usage` section with: `[STUB — full implementation in Phase 2]`

---

Create `.claude/commands/team-lead/execute.md` (creates the `/team-lead:execute` slash command):

YAML frontmatter:
- `name: team-lead:execute`
- `description: Run the full automated pipeline (Developer → CodeReview → QA → TeamLeadCheck → Done) for a single task.`
- `argument-hint: "<TASK-ID>"`
- `allowed-tools:` Read, Write, Bash, Glob, Grep, Agent

Body must include the same `## Constraints (MUST READ BEFORE PLANNING)` section with identical constraints (FOUND-06 and FOUND-07 must be visible in both stubs).

Body must include a `## Pipeline Stages` section listing: Developer → CodeReview → QA → TeamLeadCheck → Done (one line per stage).

Body must include a `## Usage` section with: `[STUB — full implementation in Phase 2]`
  </action>
  <verify>
    <automated>grep -c "10 minutes" /Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/commands/team-lead/plan.md && grep -c "fresh context\|Fresh context" /Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/commands/team-lead/execute.md && ls /Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/commands/team-lead/</automated>
  </verify>
  <acceptance_criteria>
    - .claude/commands/team-lead/plan.md exists with frontmatter field `name: team-lead:plan`
    - .claude/commands/team-lead/plan.md contains "10 minutes" (FOUND-06 constraint)
    - .claude/commands/team-lead/plan.md contains text about fresh context or isolation (FOUND-07 constraint)
    - .claude/commands/team-lead/plan.md contains "readyForDevelop" (status hook awareness)
    - .claude/commands/team-lead/execute.md exists with frontmatter field `name: team-lead:execute`
    - .claude/commands/team-lead/execute.md contains "10 minutes" (FOUND-06 constraint)
    - .claude/commands/team-lead/execute.md contains text about fresh context (FOUND-07 constraint)
    - Both files contain "[STUB — full implementation in Phase 2]"
    - ls .claude/commands/team-lead/ shows both plan.md and execute.md
  </acceptance_criteria>
  <done>/team-lead:plan and /team-lead:execute slash commands exist with FOUND-06/07 constraints declared; both marked as Phase 2 stubs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → CLAUDE.md | Claude Code loads CLAUDE.md as system context; incorrect routing rules misdirect agents |
| CLAUDE.md → sub-repo CLAUDE.md | Routing references must be accurate file paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Root CLAUDE.md content | mitigate | D-02/D-03: no tech stack, no GSD workflow details; file stays under 200 lines (routing only) |
| T-02-02 | Tampering | Slash command argument injection | accept | Stubs have no execution logic; full validation deferred to Phase 2 implementation |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | This plan installs zero external packages. No slopcheck required. |
</threat_model>

<verification>
After both tasks complete, verify:

1. `wc -l CLAUDE.md` returns less than 200.
2. `grep "NestJS\|React\|Kafka\|Prisma\|gsd\|GSD" CLAUDE.md` returns no matches (no tech stack or GSD content).
3. `grep "ai-platform/CLAUDE.md" CLAUDE.md` returns at least one match.
4. `grep "team-lead:plan" CLAUDE.md` returns at least one match.
5. `.claude/commands/team-lead/plan.md` contains "10 minutes" and "fresh context".
6. `.claude/commands/team-lead/execute.md` contains "10 minutes" and "fresh context".
</verification>

<success_criteria>
- Root CLAUDE.md is under 200 lines, contains only routing rules, references all four D-04 items
- Root CLAUDE.md contains zero NestJS/React/Kafka/Prisma/GSD content
- /team-lead:plan stub exists with ~10 min and fresh-context constraints declared
- /team-lead:execute stub exists with ~10 min and fresh-context constraints declared
- FOUND-03, FOUND-06, FOUND-07 requirements satisfied
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
