---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - ai-platform-fe/CLAUDE.md
  - ai-platform-fe/.claude/skills/fe-conventions/SKILL.md
autonomous: true
requirements:
  - FOUND-05

must_haves:
  truths:
    - "Opening ai-platform-fe/ in Claude Code loads only React FE rules — no NestJS, Kafka, or BE content"
    - "ai-platform-fe/CLAUDE.md lists an allowlist of allowed paths and references the fe-conventions skill"
    - "ai-platform-fe/.claude/skills/fe-conventions/SKILL.md exists with React/FE conventions content"
    - "ai-platform-fe/.claude/skills/ directory is created (did not exist before)"
  artifacts:
    - path: "ai-platform-fe/CLAUDE.md"
      provides: "FE isolation allowlist — path restrictions only"
      contains: "ai-platform-fe/apps"
    - path: "ai-platform-fe/.claude/skills/fe-conventions/SKILL.md"
      provides: "React/FE conventions for explicit loading by FE agents"
      contains: "Module Federation"
  key_links:
    - from: "ai-platform-fe/CLAUDE.md"
      to: "ai-platform-fe/.claude/skills/fe-conventions/SKILL.md"
      via: "Skills section reference"
      pattern: "fe-conventions/SKILL\\.md"
---

<objective>
Deliver FE isolation: create ai-platform-fe/CLAUDE.md as an isolation-first allowlist and create the fe-conventions SKILL.md that holds React/FE conventions.

Purpose: Per D-08, ai-platform-fe/CLAUDE.md must be created fresh with isolation-first content mirroring the BE CLAUDE.md pattern. The FE skills directory does not exist yet and must be created.

Output: New `ai-platform-fe/CLAUDE.md` (isolation allowlist only), new `ai-platform-fe/.claude/skills/fe-conventions/SKILL.md` (React/FE conventions).

After this plan, opening `ai-platform-fe/` in Claude Code loads only path isolation rules; FE conventions are available on demand via the skill file.
</objective>

<execution_context>
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Key patterns. Extracted from codebase. -->

From ai-platform/.claude/skills/app-structure.md (skill file format to mirror):
```
# Skill: <Name>
**Purpose:** <one-line description>
## Rules
- All comments, descriptions, and documentation must be in **English**.
## <Content Section>
```

From CLAUDE.md root (Technology Stack section — FE tech to include in fe-conventions):
React ^19, @tanstack/react-router, @tanstack/react-query, @tanstack/react-form, zod, antd, axios, @module-federation/enhanced, Vitest, Playwright, Storybook, Rspack/Rsbuild, SWC

FE app structure (from CLAUDE.md Architecture section):
- shell (MFE host): ai-platform-fe/apps/shell/
- auth (MFE remote): ai-platform-fe/apps/auth/
- chat (MFE remote): ai-platform-fe/apps/chat/
- docs (MFE remote): ai-platform-fe/apps/docs/
- @libs/api: ai-platform-fe/libs/api/
- @libs/store: ai-platform-fe/libs/store/
- @libs/ui: ai-platform-fe/libs/ui/

Locked decisions:
- D-05: Allowlist approach (list what IS allowed)
- D-08: Create fresh, isolation-first, same pattern as BE
- FOUND-05: FE-specific context and path restrictions

FE conventions from CLAUDE.md (Conventions section to extract into skill):
- File naming: PascalCase for component files (ChatMessage.tsx, LoginForm.tsx)
- Hook naming: use prefix (useConversations, useTypewriter)
- Component file naming: PascalCase
- Props interfaces: Props suffix
- Build: Rspack via nx build/serve
- Test: Vitest (unit), Playwright (e2e)
- Import aliases: @libs/ui, @libs/api, @libs/store
</interfaces>
</context>

## Phase Goal

**As a** FE agent, **I want to** open `ai-platform-fe/` and immediately know exactly which paths I am allowed to touch and where to find React conventions, **so that** I never accidentally read or write BE code or cross sub-repo boundaries.

<tasks>

<task type="auto">
  <name>Task 1: Create ai-platform-fe/CLAUDE.md as isolation-first allowlist</name>
  <files>ai-platform-fe/CLAUDE.md</files>
  <read_first>
    - ai-platform-fe/CLAUDE.md — check if file exists (it may not — per RESEARCH.md FOUND-05 "File does not exist yet"); if it exists read it first
    - ai-platform/CLAUDE.md — read the REWRITTEN version (from plan 03) as the exact structural template to mirror. Mirror the section structure and format density exactly, substituting FE paths and references.
    - .planning/phases/01-foundation/01-CONTEXT.md — D-05 (allowlist), D-08 (create fresh, isolation-first, same pattern as BE)
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 5 (CLAUDE.md sub-repo isolation allowlist style), Pitfall 6 (skills directory must be created)
    - .planning/phases/01-foundation/01-PATTERNS.md — ai-platform-fe/CLAUDE.md pattern assignment (mirror BE pattern, substitute FE paths)
  </read_first>
  <action>
Create `ai-platform-fe/CLAUDE.md` fresh with isolation-first content. Mirror the exact structure of the rewritten `ai-platform/CLAUDE.md`, substituting FE-specific paths and references throughout (per D-08).

File structure:

`# ai-platform-fe — Frontend (React)` — heading only

`## Path Isolation` — imperative statement: the agent operates ONLY within `ai-platform-fe/`. Do not read, write, or reference files outside this directory.

`## Allowed Paths` — allowlist (per D-05):
- `ai-platform-fe/apps/**` — React MFE application source (shell, auth, chat, docs)
- `ai-platform-fe/libs/**` — Shared FE libraries (@libs/api, @libs/store, @libs/ui)
- `ai-platform-fe/nx.json` — Nx workspace config
- `ai-platform-fe/tsconfig.base.json` — Shared TypeScript paths
- `ai-platform-fe/package.json` — FE workspace dependencies
- `ai-platform-fe/.env*` — environment configuration
- `ai-platform-fe/vitest.config.ts` — test configuration
- `ai-platform-fe/playwright.config.ts` — E2E test configuration

`## Skills` — single instruction: Load `ai-platform-fe/.claude/skills/fe-conventions/SKILL.md` before implementing React features.

`## Rules` — brief imperatives:
- All code comments, documentation, and descriptions must be in **English**
- Do not create new MFE apps or libs without checking ai-platform-fe/nx.json first
- Run `nx test ai-platform-fe/<app>` (Vitest) to validate unit tests before marking a task complete
- Run `nx e2e ai-platform-fe/<app>-e2e` (Playwright) for E2E tests
  </action>
  <verify>
    <automated>grep -c "Allowed Paths\|allowed paths" /Users/tarasbannyi/TestAI/ai-agent-microservices/ai-platform-fe/CLAUDE.md && grep -c "fe-conventions/SKILL.md" /Users/tarasbannyi/TestAI/ai-agent-microservices/ai-platform-fe/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - ai-platform-fe/CLAUDE.md exists
    - File contains "Allowed Paths" section
    - File contains "ai-platform-fe/apps/**" in the allowlist
    - File contains "ai-platform-fe/libs/**" in the allowlist
    - File contains "fe-conventions/SKILL.md" reference in Skills section
    - File does NOT contain "NestJS" or "Kafka" or "Prisma" (no BE content)
    - File does NOT contain "ai-platform/" (only ai-platform-fe/ paths — no cross-repo references)
  </acceptance_criteria>
  <done>ai-platform-fe/CLAUDE.md is isolation-first allowlist with FE paths only and Skills reference</done>
</task>

<task type="auto">
  <name>Task 2: Create fe-conventions/SKILL.md with React/FE conventions</name>
  <files>ai-platform-fe/.claude/skills/fe-conventions/SKILL.md</files>
  <read_first>
    - ai-platform/.claude/skills/app-structure.md — skill file format reference (# Skill:, **Purpose:**, ## Rules, content sections). The fe-conventions skill must follow this exact format.
    - ai-platform/.claude/skills/be-conventions/SKILL.md — read the newly created BE skill as a structural reference; mirror the section organization for FE
    - CLAUDE.md (root) — Conventions section (naming patterns, code style, frontend-specific patterns), Technology Stack FE section (exact library names and versions to include), Architecture component responsibilities table for FE components
    - .planning/phases/01-foundation/01-PATTERNS.md — fe-conventions/SKILL.md pattern assignment (React/FE conventions to include)
  </read_first>
  <action>
Create `ai-platform-fe/.claude/skills/fe-conventions/SKILL.md`. The `ai-platform-fe/.claude/skills/` directory does not exist — creating this file will create it automatically. Follow the skill file format from app-structure.md exactly.

File structure:

`# Skill: FE Conventions (React)` — heading

`**Purpose:**` Load this skill before implementing React features in ai-platform-fe/.

`## Rules`
- All comments, descriptions, and documentation must be in **English**.

`## App Structure`
Directory tree showing:
```
ai-platform-fe/
├── apps/
│   ├── shell/          # MFE host — TanStack Router, QueryClientProvider, lazy-loads remotes
│   ├── auth/           # MFE remote — Login, Register, Google OAuth, GuestRoute
│   ├── chat/           # MFE remote — Chat page, useChat hook, SSE client, conversation sidebar
│   └── docs/           # MFE remote — placeholder
└── libs/
    ├── api/            # Axios client, API endpoint functions, SSE streamMessage, types (@libs/api)
    ├── store/          # TanStack Query hooks for conversations, useActiveConversation (@libs/store)
    └── ui/             # Shared React components, Storybook (@libs/ui)
```

`## Module Federation Pattern`
- shell is the MFE host (Webpack/Rspack Module Federation host)
- auth, chat, docs are MFE remotes lazy-loaded at runtime via @module-federation/enhanced
- Import from remotes via @libs/api, @libs/store, @libs/ui aliases (NOT direct relative paths across apps)

`## Naming Conventions`
- Component files: PascalCase — `ChatMessage.tsx`, `LoginForm.tsx`, `AppLayout.tsx`
- Hook files: camelCase with `use` prefix — `useConversations.ts`, `useTypewriter.ts`
- Story files: `ComponentName.stories.tsx` co-located with component
- Test files: `*.spec.tsx` co-located with subject file
- Props interfaces: `<Component>Props` suffix — `ChatMessageProps`, `LoginFormProps`
- Form value types: `<Feature>Values` suffix — `LoginFormValues`
- Constants: `SCREAMING_SNAKE_CASE` for module-level values

`## Key Conventions`
- **Build**: `nx serve <app>` (Rspack dev server) / `nx build <app>` (production build)
- **Unit test**: `nx test <app|lib>` — Vitest
- **E2E test**: `nx e2e <app>-e2e` — Playwright
- **Import aliases**: `@libs/api`, `@libs/store`, `@libs/ui` — defined in vitest.config.ts resolve aliases
- **State**: TanStack Query (`@tanstack/react-query`) for server state; `useSyncExternalStore` for module-level singletons
- **Forms**: `@tanstack/react-form` + `@tanstack/zod-form-adapter` + `zod` for validation
- **UI components**: Ant Design (`antd` ^6) — use `message.error()` for user-facing error messages
- **HTTP client**: axios (configured in `ai-platform-fe/libs/api/src/client.ts`) — not fetch
- **Linting**: TypeScript ESLint (`eslint.config.mjs`) + eslint-plugin-react, eslint-plugin-react-hooks
- **Commit**: commitlint with @commitlint/config-conventional

`## Environment Variables`
- Must be prefixed `NX_PUBLIC_` to be inlined by Rspack into browser bundle
- `NX_PUBLIC_API_URL` — API gateway base URL (default: http://localhost:4000/api)
  </action>
  <verify>
    <automated>grep -c "Skill: FE Conventions\|# Skill:" /Users/tarasbannyi/TestAI/ai-agent-microservices/ai-platform-fe/.claude/skills/fe-conventions/SKILL.md && grep -c "Module Federation\|module-federation" /Users/tarasbannyi/TestAI/ai-agent-microservices/ai-platform-fe/.claude/skills/fe-conventions/SKILL.md && ls /Users/tarasbannyi/TestAI/ai-agent-microservices/ai-platform-fe/.claude/skills/</automated>
  </verify>
  <acceptance_criteria>
    - ai-platform-fe/.claude/skills/ directory exists (confirmed by ls output)
    - ai-platform-fe/.claude/skills/fe-conventions/SKILL.md exists
    - File starts with `# Skill: FE Conventions` (or similar)
    - File contains `**Purpose:**` one-liner
    - File contains `## Rules` with English requirement
    - File contains "Module Federation" reference
    - File contains "@libs/api", "@libs/store", "@libs/ui" import alias references
    - File contains Vitest and Playwright test command references (`nx test`, `nx e2e`)
    - File contains naming convention section with PascalCase and `use` prefix references
    - File contains `NX_PUBLIC_` env var prefix rule
  </acceptance_criteria>
  <done>fe-conventions/SKILL.md contains full React/FE conventions; ai-platform-fe/.claude/skills/ directory created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → ai-platform-fe/ | Sub-repo CLAUDE.md constrains agent path scope to FE directories only |
| CLAUDE.md → skills | FE skills must be explicitly loaded — not auto-injected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | FE agent accessing BE paths | mitigate | Allowlist in ai-platform-fe/CLAUDE.md lists only ai-platform-fe/** paths; no ai-platform/ appears in the file |
| T-04-02 | Information Disclosure | FE conventions skill auto-loaded | accept | Skills are not auto-loaded by Claude Code; agents must explicitly reference the skill path. Low risk. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | This plan installs zero external packages. No slopcheck required. |
</threat_model>

<verification>
After both tasks complete, verify:

1. `grep "NestJS\|Kafka\|Prisma" ai-platform-fe/CLAUDE.md` returns no matches (no BE content).
2. `grep "Allowed Paths" ai-platform-fe/CLAUDE.md` returns at least one match.
3. `grep "fe-conventions/SKILL.md" ai-platform-fe/CLAUDE.md` returns at least one match.
4. `ls ai-platform-fe/.claude/skills/` shows the fe-conventions directory.
5. `grep "Module Federation" ai-platform-fe/.claude/skills/fe-conventions/SKILL.md` returns at least one match.
6. `grep "ai-platform/" ai-platform-fe/CLAUDE.md` returns no matches (no BE paths in FE isolation file).
</verification>

<success_criteria>
- ai-platform-fe/CLAUDE.md is isolation-first with FE allowlist and Skills reference only
- ai-platform-fe/CLAUDE.md contains zero BE/NestJS content
- ai-platform-fe/.claude/skills/ directory created
- ai-platform-fe/.claude/skills/fe-conventions/SKILL.md exists with full React/FE conventions (app structure, MFE pattern, naming, key conventions, env vars)
- FOUND-05 requirement satisfied
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done
</output>
