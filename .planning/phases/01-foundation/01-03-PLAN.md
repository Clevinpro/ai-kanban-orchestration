---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - ai-platform/CLAUDE.md
  - ai-platform/.claude/skills/be-conventions/SKILL.md
autonomous: true
requirements:
  - FOUND-04

must_haves:
  truths:
    - "Opening ai-platform/ in Claude Code loads only NestJS BE rules — no React, Kafka consumer product logic, or GSD workflow content"
    - "ai-platform/CLAUDE.md lists an allowlist of allowed paths (not a blocklist) and references the be-conventions skill"
    - "The NestJS/BE conventions that were in ai-platform/CLAUDE.md are moved to the be-conventions SKILL.md"
    - "ai-platform/.claude/skills/be-conventions/SKILL.md exists with NestJS conventions content"
  artifacts:
    - path: "ai-platform/CLAUDE.md"
      provides: "BE isolation allowlist — path restrictions only"
      contains: "ai-platform/apps"
    - path: "ai-platform/.claude/skills/be-conventions/SKILL.md"
      provides: "NestJS conventions for explicit loading by BE agents"
      contains: "NestJS"
  key_links:
    - from: "ai-platform/CLAUDE.md"
      to: "ai-platform/.claude/skills/be-conventions/SKILL.md"
      via: "Skills section reference"
      pattern: "be-conventions/SKILL\\.md"
---

<objective>
Deliver BE isolation: rewrite ai-platform/CLAUDE.md as an isolation-first allowlist and create the be-conventions SKILL.md that holds the NestJS conventions that previously lived in the CLAUDE.md.

Purpose: Per D-06/D-07, the sub-repo CLAUDE.md must be isolation-first — it defines what BE agents are allowed to touch. Conventions move to an explicit-load skill file so BE agents load them when needed, not always.

Output: Rewritten `ai-platform/CLAUDE.md` (isolation allowlist only), new `ai-platform/.claude/skills/be-conventions/SKILL.md` (NestJS conventions).

After this plan, opening `ai-platform/` in Claude Code loads only path isolation rules; BE conventions are available on demand via the skill file.
</objective>

<execution_context>
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Key patterns. Extracted from codebase. -->

From ai-platform/.claude/skills/app-structure.md (skill file format):
```
# Skill: <Name>
**Purpose:** <one-line description>
## Rules
- All comments, descriptions, and documentation must be in **English**.
## <Content Section>
```
Every skill starts with `# Skill:`, `**Purpose:**` one-liner, `## Rules` with English rule, then content sections.

From ai-platform/CLAUDE.md current content (lines to extract into be-conventions/SKILL.md):
- Service pattern tree (apps/<name>/src/ structure)
- Key conventions bullet list: monorepo scope @ai-platform/*, build/test/lint commands, module pattern, validation, logging, linting, commit
- Project layout tree (apps/, libs/, prisma/)

Locked decisions:
- D-05: Allowlist approach — list what agents ARE ALLOWED to touch
- D-06: Isolation-first — allowlist section is primary content; existing NestJS conventions removed from CLAUDE.md
- D-07: NestJS conventions move to ai-platform/.claude/skills/be-conventions/SKILL.md (explicit load)
</interfaces>
</context>

## Phase Goal

**As a** BE agent, **I want to** open `ai-platform/` and immediately know exactly which paths I am allowed to touch and where to find NestJS conventions, **so that** I never accidentally read or write FE code or cross sub-repo boundaries.

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ai-platform/CLAUDE.md as isolation-first allowlist</name>
  <files>ai-platform/CLAUDE.md</files>
  <read_first>
    - ai-platform/CLAUDE.md — read the FULL current file before rewriting it. Pay special attention to lines 26-56 (service pattern tree, key conventions bullet list): mentally retain this content because Task 2 will use it to populate be-conventions/SKILL.md. Do NOT proceed to rewrite until you have captured the conventions content in your working context.
    - .planning/phases/01-foundation/01-CONTEXT.md — D-05 (allowlist not blocklist), D-06 (isolation-first, conventions removed), D-07 (conventions move to skill)
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 5 (CLAUDE.md sub-repo isolation allowlist style template) and "Sub-repo CLAUDE.md allowlist wording" under Pitfall 6
    - .planning/phases/01-foundation/01-PATTERNS.md — ai-platform/CLAUDE.md rewrite pattern assignment (new content structure, allowed paths list)
    - CLAUDE.md (rewritten in plan 02) — read as format density reference after plan 02 completes
  </read_first>
  <action>
Rewrite `ai-platform/CLAUDE.md` from scratch with isolation-first content. The rewritten file must follow this structure exactly (per D-05, D-06):

`# ai-platform — Backend (NestJS)` — heading only, no subtitle

`## Path Isolation` — imperative statement that the agent operates ONLY within `ai-platform/`. Must explicitly state: do not read, write, or reference files outside this directory.

`## Allowed Paths` — allowlist (per D-05, list what IS ALLOWED):
- `ai-platform/apps/**` — NestJS application source (api-gateway, auth-service, ai-service)
- `ai-platform/libs/**` — Shared NestJS libraries (database, kafka, shared)
- `ai-platform/prisma/**` (or `ai-platform/libs/database/prisma/**` — check actual location in codebase) — Prisma schema and migrations
- `ai-platform/docker-compose.yml` — Local infrastructure
- `ai-platform/nx.json`, `ai-platform/tsconfig.base.json`, `ai-platform/package.json` — workspace config files
- `ai-platform/.env*` — environment configuration

`## Skills` — single instruction: Load `ai-platform/.claude/skills/be-conventions/SKILL.md` before implementing NestJS features.

`## Rules` — brief imperatives:
- All code comments, documentation, and descriptions must be in **English**
- Do not create new NestJS apps or libs without checking ai-platform/nx.json first
- Run `nx test ai-platform/<app>` to validate before marking a task complete

The existing NestJS conventions content (service pattern, key conventions list — currently lines 26-56) must NOT appear in this file — those move to be-conventions/SKILL.md in Task 2.
  </action>
  <verify>
    <automated>grep -c "Allowed Paths\|allowed paths" /Users/tarasbannyi/TestAI/ai-agent-microservices/ai-platform/CLAUDE.md && grep -c "be-conventions/SKILL.md" /Users/tarasbannyi/TestAI/ai-agent-microservices/ai-platform/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - ai-platform/CLAUDE.md contains "Allowed Paths" or "allowed paths" section header
    - ai-platform/CLAUDE.md contains "ai-platform/apps/**" in the allowlist
    - ai-platform/CLAUDE.md contains "ai-platform/libs/**" in the allowlist
    - ai-platform/CLAUDE.md contains "be-conventions/SKILL.md" reference in Skills section
    - ai-platform/CLAUDE.md does NOT contain "Service pattern (every app follows this)" (conventions moved to skill)
    - ai-platform/CLAUDE.md does NOT contain "Key conventions" section (conventions moved to skill)
    - ai-platform/CLAUDE.md does NOT contain text about ai-platform-fe (no cross-repo content)
  </acceptance_criteria>
  <done>ai-platform/CLAUDE.md is isolation-first allowlist with Skills reference; NestJS conventions removed to be-conventions/SKILL.md</done>
</task>

<task type="auto">
  <name>Task 2: Create be-conventions/SKILL.md with NestJS conventions</name>
  <files>ai-platform/.claude/skills/be-conventions/SKILL.md</files>
  <read_first>
    - ai-platform/.claude/skills/app-structure.md — skill file format reference: `# Skill: <Name>`, `**Purpose:**`, `## Rules`, content sections. Replicate this format exactly.
    - .planning/phases/01-foundation/01-PATTERNS.md — be-conventions/SKILL.md pattern assignment (content to include: service pattern, monorepo scope, build/test commands, module pattern)
    - CLAUDE.md (root, from plan 02 output) — technology stack section covers NestJS libraries used; reference for what conventions to include in the skill
  </read_first>
  <action>
Create `ai-platform/.claude/skills/be-conventions/SKILL.md`. Use the NestJS conventions content you captured from lines 26-56 of the original ai-platform/CLAUDE.md when reading it in Task 1. Follow the skill file format from app-structure.md exactly.

File structure:

`# Skill: BE Conventions (NestJS)` — heading

`**Purpose:**` Load this skill before implementing NestJS features in ai-platform/.

`## Rules`
- All comments, descriptions, and documentation must be in **English**.

`## Project Layout`
Include the full directory tree from the current ai-platform/CLAUDE.md (apps/, libs/ with sub-libs, prisma/, nx.json, package.json, tsconfig.base.json). This is a direct lift from lines 11-24 of the current ai-platform/CLAUDE.md.

`## Service Pattern`
Include the apps/<name>/src/ structure tree (main.ts, app/, <feature>/ pattern). This is a direct lift from lines 28-42 of the current ai-platform/CLAUDE.md.

`## Key Conventions`
Include all bullet points from the "Key conventions" section of the current ai-platform/CLAUDE.md (lines 44-56):
- **Monorepo scope**: `@ai-platform/*` — libs use `@ai-platform/database`, `@ai-platform/kafka`, `@ai-platform/shared`
- **Build**: `nx serve <app>` / `nx build <lib>`
- **Test**: `nx test <app|lib>` — Jest
- **Deps**: libs declare deps on other libs via package.json dependencies
- **Modules**: Every feature is a self-contained NestJS module with controller → service → DTOs
- **Validation**: class-transformer + class-validator DTOs
- **Logging**: pino via nestjs-pino (from @ai-platform/shared)
- **Linting**: TypeScript ESLint + eslint-config-prettier
- **Commit**: commitlint with @commitlint/config-conventional
- **Run local env**: `npm start` launches all 3 services in parallel

`## App Dependencies`
Include the App dependencies table from app-structure.md (api-gateway, auth-service, ai-service internal deps).
  </action>
  <verify>
    <automated>grep -c "Skill: BE Conventions\|# Skill:" /Users/tarasbannyi/TestAI/ai-agent-microservices/ai-platform/.claude/skills/be-conventions/SKILL.md && grep -c "nx test\|nx serve" /Users/tarasbannyi/TestAI/ai-agent-microservices/ai-platform/.claude/skills/be-conventions/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - ai-platform/.claude/skills/be-conventions/SKILL.md exists
    - File starts with `# Skill: BE Conventions` (or similar)
    - File contains `**Purpose:**` one-liner
    - File contains `## Rules` with English requirement
    - File contains `nx test` command reference
    - File contains `@ai-platform/database`, `@ai-platform/kafka`, `@ai-platform/shared` monorepo scope references
    - File contains service pattern directory tree (main.ts, app.module.ts, feature.controller.ts structure)
    - File contains "class-transformer" and "class-validator" convention reference
  </acceptance_criteria>
  <done>be-conventions/SKILL.md contains full NestJS conventions lifted from old ai-platform/CLAUDE.md; agents can load it explicitly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → ai-platform/ | Sub-repo CLAUDE.md constrains agent path scope to BE directories only |
| CLAUDE.md → skills | Skills must be explicitly loaded — they are not auto-injected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | BE agent accessing FE paths | mitigate | Allowlist in ai-platform/CLAUDE.md explicitly lists only ai-platform/** paths; no ai-platform-fe/ appears anywhere in the file |
| T-03-02 | Information Disclosure | Conventions skill auto-loaded | accept | Skills are not auto-loaded by Claude Code; agents must explicitly reference the skill path. Low risk. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | This plan installs zero external packages. No slopcheck required. |
</threat_model>

<verification>
After both tasks complete, verify:

1. `grep "Service pattern\|Key conventions" ai-platform/CLAUDE.md` returns no matches (conventions moved).
2. `grep "Allowed Paths" ai-platform/CLAUDE.md` returns at least one match.
3. `grep "be-conventions/SKILL.md" ai-platform/CLAUDE.md` returns at least one match.
4. `grep "nx test" ai-platform/.claude/skills/be-conventions/SKILL.md` returns at least one match.
5. `grep "ai-platform-fe" ai-platform/CLAUDE.md` returns no matches (no cross-repo content).
</verification>

<success_criteria>
- ai-platform/CLAUDE.md is isolation-first with allowlist and Skills reference only
- ai-platform/CLAUDE.md contains zero NestJS conventions (all moved to skill file)
- ai-platform/.claude/skills/be-conventions/SKILL.md exists with full NestJS conventions (layout, service pattern, key conventions, app dependencies)
- FOUND-04 requirement satisfied
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
