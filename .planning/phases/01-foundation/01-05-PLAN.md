---
phase: 01-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/hooks/task-state-guard.js
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - FOUND-02
  - FOUND-04
  - FOUND-05
gap_closure: true

must_haves:
  truths:
    - "Editing a task file status from readyForDevelop to inProgress via an Edit call injects the current ISO timestamp into the updated-at field — confirmed by inspecting modifiedInput.new_string in hook output"
    - "The Write allow path continues to return modifiedInput.content (not new_string) — Write behavior is unchanged"
    - "FOUND-04 and FOUND-05 are marked [x] and status 'Complete' in REQUIREMENTS.md"
    - "ROADMAP.md Phase 1 Success Criteria item 2 describes the correct hook type: PreToolUse (not PostToolUse)"
  artifacts:
    - path: ".claude/hooks/task-state-guard.js"
      provides: "PreToolUse hook with correct Edit allow path returning modifiedInput.new_string"
      contains: "new_string: updatedNewString"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirements tracking with FOUND-04 and FOUND-05 marked complete"
      contains: "[x] **FOUND-04**"
    - path: ".planning/ROADMAP.md"
      provides: "Roadmap with correct PreToolUse label in SC-2"
      contains: "PreToolUse hook"
  key_links:
    - from: ".claude/hooks/task-state-guard.js"
      to: "Edit tool modifiedInput"
      via: "modifiedInput.new_string (Edit path) vs modifiedInput.content (Write path)"
      pattern: "new_string: updatedNewString"
---

<objective>
Close three verification gaps from the Phase 1 verification report: fix the Edit allow path in task-state-guard.js so the timestamp injection is not silently discarded (CR-01 blocker), update REQUIREMENTS.md to mark FOUND-04 and FOUND-05 complete (documentation gap), and correct the hook-type label in ROADMAP.md SC-2 from PostToolUse to PreToolUse (wording error).

Purpose: CR-01 means that every status transition made via an Edit call — the normal operational case — silently drops the updated-at timestamp injection. Downstream phases that sort or filter tasks by freshness will see stale timestamps. The documentation gaps cause REQUIREMENTS.md and ROADMAP.md to misrepresent the actual implementation state.

Output: Corrected task-state-guard.js (Edit allow path returns modifiedInput.new_string), updated REQUIREMENTS.md (FOUND-04/05 marked [x] and Complete), corrected ROADMAP.md (SC-2 says PreToolUse).
</objective>

<execution_context>
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-VERIFICATION.md

<interfaces>
<!-- Key facts about the current hook implementation extracted from the file. -->

From .claude/hooks/task-state-guard.js (current state, lines 85-97):

The ALLOW path currently builds a single modifiedInput for both Write and Edit:

  const now = new Date().toISOString();
  const updatedContent = finalContent.replace(/(updated-at:\s*)([^\n]+)/, `$1${now}`);

  const output = {
    hookSpecificOutput: {
      hookEventName: 'PreToolUse',
      permissionDecision: 'allow',
    },
    modifiedInput: {
      content: updatedContent,   // BUG: Edit tool ignores 'content'; it reads 'new_string'
    },
  };

The Edit allow path must instead apply the timestamp regex to tool_input.new_string
(not to finalContent) and return modifiedInput.new_string (not modifiedInput.content):

  // Edit path
  const updatedNewString = (tool_input.new_string || '').replace(/(updated-at:\s*)([^\n]+)/, `$1${now}`);
  modifiedInput = { new_string: updatedNewString };

  // Write path (unchanged)
  const updatedContent = (tool_input.content || '').replace(/(updated-at:\s*)([^\n]+)/, `$1${now}`);
  modifiedInput = { content: updatedContent };

Note: finalContent (the reconstructed full file used for repo check in step 7) must NOT be
used as the value for modifiedInput on either path. For Edit, the Edit tool applies
modifiedInput.new_string as a replacement for old_string in the existing file — so only the
portion being replaced is returned, with the timestamp stamped in that portion.

The existing variable 'finalContent' in the hook is computed at line 77 for the repo check
at lines 80-83. It is NOT used in modifiedInput. Keep finalContent for repo validation only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Edit allow path in task-state-guard.js to return modifiedInput.new_string</name>
  <files>.claude/hooks/task-state-guard.js</files>
  <read_first>
    - .claude/hooks/task-state-guard.js — read the full file before editing; focus on lines 85-100 (the ALLOW path) and lines 71-83 (finalContent construction and repo check). The ALLOW path currently uses a single updatedContent variable derived from finalContent and always returns modifiedInput.content. This must become two conditional branches keyed on tool_name.
    - .planning/phases/01-foundation/01-VERIFICATION.md — lines 8-15 (gaps YAML block) and lines 148-156 (gap analysis section): understand the exact bug and the prescribed fix before touching the file.
  </read_first>
  <action>
Edit `.claude/hooks/task-state-guard.js` to split the ALLOW path (lines 85-100) into two branches based on tool_name.

Current code to replace (the entire ALLOW block starting at the comment on line 85):

    // 8. ALLOW path — inject updated-at into the authoritative final content
    const now = new Date().toISOString();
    const updatedContent = finalContent.replace(/(updated-at:\s*)([^\n]+)/, `$1${now}`);

    const output = {
      hookSpecificOutput: {
        hookEventName: 'PreToolUse',
        permissionDecision: 'allow',
      },
      modifiedInput: {
        content: updatedContent,
      },
    };

    process.stdout.write(JSON.stringify(output));
    process.exit(0);

Replace with:

    // 8. ALLOW path — inject updated-at, keyed by tool_name
    const now = new Date().toISOString();
    let modifiedInput;
    if (tool_name === 'Write') {
      // Write tool: modifiedInput.content is the complete file content to write
      const updatedContent = (tool_input.content || '').replace(/(updated-at:\s*)([^\n]+)/, `$1${now}`);
      modifiedInput = { content: updatedContent };
    } else {
      // Edit tool: modifiedInput.new_string is the replacement snippet (not full file)
      // The timestamp regex is applied to the new_string portion being substituted
      const updatedNewString = (tool_input.new_string || '').replace(/(updated-at:\s*)([^\n]+)/, `$1${now}`);
      modifiedInput = { new_string: updatedNewString };
    }

    const output = {
      hookSpecificOutput: {
        hookEventName: 'PreToolUse',
        permissionDecision: 'allow',
      },
      modifiedInput,
    };

    process.stdout.write(JSON.stringify(output));
    process.exit(0);

Do NOT touch any other part of the file: the stdin boilerplate (lines 1-24), guard clause (line 30), path filter (line 34), status extraction (lines 37-44), new-file check (lines 56-59), transition check (lines 62-68), finalContent construction (lines 72-78), or repo check (lines 80-83) are all correct and must remain unchanged.

Also fix the stray comment on line 29 ("only Watch Write and Edit") — change "only Watch Write and Edit" to "only handle Write and Edit". This is a cosmetic fix identified in the verification report (anti-pattern: Info severity, IN-04).
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const src = fs.readFileSync('/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/hooks/task-state-guard.js', 'utf8');
const hasNewString = src.includes(\"new_string: updatedNewString\");
const hasContentPath = src.includes(\"content: updatedContent\");
const hasToolNameBranch = src.includes(\"tool_name === 'Write'\");
const noFlatContent = !src.match(/modifiedInput\s*=\s*\{\s*content:\s*updatedContent\s*\}[^}]*\}\s*;?\s*process\.stdout/s);
console.log('new_string branch:', hasNewString);
console.log('content branch:', hasContentPath);
console.log('tool_name branch:', hasToolNameBranch);
console.log('OVERALL:', (hasNewString && hasContentPath && hasToolNameBranch) ? 'PASS' : 'FAIL');
"</automated>
  </verify>
  <acceptance_criteria>
    - task-state-guard.js contains `tool_name === 'Write'` branch in the ALLOW section
    - task-state-guard.js contains `new_string: updatedNewString` (Edit allow path key)
    - task-state-guard.js contains `content: updatedContent` (Write allow path key)
    - task-state-guard.js does NOT contain a bare `modifiedInput: { content: updatedContent }` that covers both tool types at once (the old flat structure is gone)
    - Live smoke test (Edit path): pipe the following JSON to the hook and confirm the output contains `"new_string"` key and a timestamp in updated-at:
      Input: `{"tool_name":"Edit","tool_input":{"file_path":"/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/work/test-epic/TASK-001.md","old_string":"status: readyForDevelop\nupdated-at: 2026-01-01T00:00:00.000Z","new_string":"status: inProgress\nupdated-at: 2026-01-01T00:00:00.000Z"}}`
      Expected: output JSON contains `permissionDecision: "allow"` and `modifiedInput.new_string` includes a fresh ISO timestamp in the updated-at line (not the original 2026-01-01 value)
    - Live smoke test (Write path): pipe a Write JSON with repo:be and status:readyForDevelop and confirm output contains `modifiedInput.content` (not new_string) with a fresh timestamp in updated-at
    - All previously-passing behaviors remain intact: invalid transition denial, repo:both denial, new file with status!=readyForDevelop denial, path filter (non-.planning/work/ files exit 0)
  </acceptance_criteria>
  <done>The Edit allow path returns modifiedInput.new_string with the timestamp regex applied to tool_input.new_string; the Write allow path returns modifiedInput.content unchanged from current behavior; no other hook logic is modified</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md and ROADMAP.md to close documentation gaps</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — read lines 13-16 (FOUND-04, FOUND-05 checkboxes) and lines 83-87 (Traceability table rows for FOUND-04 and FOUND-05): these are the two locations that must be updated
    - .planning/ROADMAP.md — read lines 33-37 (Phase 1 Success Criteria items 1-2): item 2 contains the string "PostToolUse hook" that must become "PreToolUse hook"
    - .planning/phases/01-foundation/01-VERIFICATION.md — lines 108-119 (Requirements Coverage table): confirms FOUND-04 and FOUND-05 are "SATISFIED (code complete; REQUIREMENTS.md checkbox not updated)" — implementation exists, only documentation needs fixing
  </read_first>
  <action>
Make three targeted edits — two in REQUIREMENTS.md and one in ROADMAP.md.

REQUIREMENTS.md edit 1 — Foundation checklist (lines 13-14): Change the two unchecked items to checked:

  Old:
    - [ ] **FOUND-04**: `ai-platform/CLAUDE.md` created — NestJS/BE-specific context and path restrictions
    - [ ] **FOUND-05**: `ai-platform-fe/CLAUDE.md` created — React/FE-specific context and path restrictions

  New:
    - [x] **FOUND-04**: `ai-platform/CLAUDE.md` created — NestJS/BE-specific context and path restrictions
    - [x] **FOUND-05**: `ai-platform-fe/CLAUDE.md` created — React/FE-specific context and path restrictions

REQUIREMENTS.md edit 2 — Traceability table (lines 84-85): Change status from "Pending" to "Complete" for FOUND-04 and FOUND-05 rows:

  Old:
    | FOUND-04 | Phase 1 | Pending |
    | FOUND-05 | Phase 1 | Pending |

  New:
    | FOUND-04 | Phase 1 | Complete |
    | FOUND-05 | Phase 1 | Complete |

ROADMAP.md edit — Phase 1 Success Criteria item 2 (line ~35): Change "PostToolUse hook" to "PreToolUse hook":

  Old:
    2. Writing an invalid status transition to a task file's frontmatter is blocked by the PostToolUse hook

  New:
    2. Writing an invalid status transition to a task file's frontmatter is blocked by the PreToolUse hook

Do not change any other content in either file. Do not re-order items, add explanatory text, or modify the "Last updated" date — keep it as-is.
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const req = fs.readFileSync('/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/REQUIREMENTS.md', 'utf8');
const road = fs.readFileSync('/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/ROADMAP.md', 'utf8');
const found04Checked = req.includes('[x] **FOUND-04**');
const found05Checked = req.includes('[x] **FOUND-05**');
const found04Complete = req.match(/FOUND-04\s*\|\s*Phase 1\s*\|\s*Complete/);
const found05Complete = req.match(/FOUND-05\s*\|\s*Phase 1\s*\|\s*Complete/);
const roadmapFixed = road.includes('PreToolUse hook') && !road.includes('PostToolUse hook');
console.log('FOUND-04 checkbox:', found04Checked);
console.log('FOUND-05 checkbox:', found05Checked);
console.log('FOUND-04 traceability:', !!found04Complete);
console.log('FOUND-05 traceability:', !!found05Complete);
console.log('ROADMAP SC-2 label:', roadmapFixed);
console.log('OVERALL:', (found04Checked && found05Checked && found04Complete && found05Complete && roadmapFixed) ? 'PASS' : 'FAIL');
"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md line for FOUND-04 reads `- [x] **FOUND-04**: ...` (checkbox is checked)
    - REQUIREMENTS.md line for FOUND-05 reads `- [x] **FOUND-05**: ...` (checkbox is checked)
    - REQUIREMENTS.md Traceability table row for FOUND-04 shows status "Complete" (not "Pending")
    - REQUIREMENTS.md Traceability table row for FOUND-05 shows status "Complete" (not "Pending")
    - ROADMAP.md Phase 1 Success Criteria item 2 contains the string "PreToolUse hook" and does NOT contain "PostToolUse hook" anywhere in the file
    - No other content in REQUIREMENTS.md or ROADMAP.md is altered — all other checkboxes, table rows, dates, and headings remain identical to their pre-edit state
  </acceptance_criteria>
  <done>FOUND-04 and FOUND-05 are marked complete in both the Foundation checklist and the Traceability table; ROADMAP.md SC-2 correctly labels the hook as PreToolUse</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hook output → Edit tool | Hook returns modifiedInput; Edit tool reads only the key it recognises (new_string for Edit, content for Write). Wrong key = silent drop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-GC-01 | Tampering | task-state-guard.js Edit allow path | mitigate | Fix uses modifiedInput.new_string for Edit calls — Claude Code Edit tool will now consume the hook's timestamp injection instead of silently discarding it |
| T-01-GC-02 | Tampering | task-state-guard.js Write allow path | accept | Write path continues to use modifiedInput.content — no change; already verified working by live test in 01-VERIFICATION.md |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | This plan installs zero packages. No slopcheck required. |
</threat_model>

<verification>
After both tasks complete, run the following checks:

1. Edit path timestamp injection (CR-01 fix):
   First create a test file: `mkdir -p /Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/work/test-epic && printf -- '---\nid: TASK-001\ntitle: Test\nstatus: readyForDevelop\npriority: low\nrepo: be\nepic: test-epic\ncomplexity: 1\ncreated-at: 2026-01-01T00:00:00.000Z\nupdated-at: 2026-01-01T00:00:00.000Z\n---\n' > /Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/work/test-epic/TASK-001.md`
   Then pipe an Edit JSON to the hook:
   `echo '{"tool_name":"Edit","tool_input":{"file_path":"/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/work/test-epic/TASK-001.md","old_string":"status: readyForDevelop\nupdated-at: 2026-01-01T00:00:00.000Z","new_string":"status: inProgress\nupdated-at: 2026-01-01T00:00:00.000Z"}}' | node /Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/hooks/task-state-guard.js`
   Expected output: JSON with `"permissionDecision":"allow"` and `"modifiedInput":{"new_string":"status: inProgress\nupdated-at: 2026-..."}` where the timestamp is the current time (not 2026-01-01).

2. Write path unchanged:
   `echo '{"tool_name":"Write","tool_input":{"file_path":"/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/work/test-epic/TASK-002.md","content":"---\nid: TASK-002\ntitle: Test\nstatus: readyForDevelop\npriority: low\nrepo: be\nepic: test-epic\ncomplexity: 1\ncreated-at: 2026-01-01T00:00:00.000Z\nupdated-at: 2026-01-01T00:00:00.000Z\n---\n"}}' | node /Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/hooks/task-state-guard.js`
   Expected output: JSON with `"permissionDecision":"allow"` and `"modifiedInput":{"content":"..."}` (content key, not new_string).

3. REQUIREMENTS.md: `grep -c '\[x\] \*\*FOUND-04' /Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/REQUIREMENTS.md` returns 1; same for FOUND-05.

4. ROADMAP.md: `grep -c 'PreToolUse hook' /Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/ROADMAP.md` returns 1; `grep -c 'PostToolUse hook' /Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/ROADMAP.md` returns 0.
</verification>

<success_criteria>
- .claude/hooks/task-state-guard.js Edit allow path returns modifiedInput.new_string with timestamp injected into tool_input.new_string — verified by live pipe test showing new_string key in output
- .claude/hooks/task-state-guard.js Write allow path returns modifiedInput.content (unchanged from pre-fix) — verified by live pipe test showing content key in output
- .claude/hooks/task-state-guard.js all existing denial behaviors unchanged (invalid transition, repo:both, wrong initial status, path filter) — verified by existing acceptance_criteria smoke tests
- .planning/REQUIREMENTS.md FOUND-04 shows [x] in checklist and "Complete" in Traceability table
- .planning/REQUIREMENTS.md FOUND-05 shows [x] in checklist and "Complete" in Traceability table
- .planning/ROADMAP.md Phase 1 SC-2 reads "PreToolUse hook" — "PostToolUse hook" string is absent from the entire file
- FOUND-02, FOUND-04, FOUND-05 requirement IDs are satisfied
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-05-SUMMARY.md` when done
</output>
