---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/task-schema.yaml
  - .planning/work/.gitkeep
  - .claude/hooks/task-state-guard.js
  - .claude/settings.json
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02

must_haves:
  truths:
    - "Writing a valid TASK-XXX.md with all required frontmatter fields and status: readyForDevelop is allowed by the hook"
    - "Writing a TASK-XXX.md with repo: both is denied by the hook"
    - "Transitioning a task status to a non-adjacent state (e.g., readyForDevelop -> done) is denied by the hook"
    - "A valid status transition (e.g., readyForDevelop -> inProgress) is allowed by the hook"
    - "On every allowed write, the hook injects the current ISO timestamp into the updated-at frontmatter field via modifiedInput"
    - "The .planning/work/ directory exists as the task file root"
    - "The task-schema.yaml documents all required fields and the full six-state lifecycle"
  artifacts:
    - path: ".planning/task-schema.yaml"
      provides: "Machine-readable task schema with fields and lifecycle transitions"
      contains: "version: 1"
    - path: ".planning/work/.gitkeep"
      provides: "Task file root directory (placeholder)"
    - path: ".claude/hooks/task-state-guard.js"
      provides: "PreToolUse hook that validates YAML frontmatter transitions and injects updated-at"
      contains: "permissionDecision"
    - path: ".claude/settings.json"
      provides: "Hook registration config"
      contains: "task-state-guard.js"
  key_links:
    - from: ".claude/settings.json"
      to: ".claude/hooks/task-state-guard.js"
      via: "PreToolUse command entry"
      pattern: "task-state-guard\\.js"
    - from: ".claude/hooks/task-state-guard.js"
      to: ".planning/work/**/*.md"
      via: "filePath.includes('.planning/work/')"
      pattern: "\\.planning/work/"
---

<objective>
Deliver the task file schema and enforcement hook — the structural contract for all task files in the system.

Purpose: Without the schema and hook, downstream phases have no agreed-upon format for task files and no enforcement of the status lifecycle. This is the foundation that makes the entire pipeline possible.

Output: `.planning/task-schema.yaml` (machine-readable schema), `.planning/work/` directory (task root), `task-state-guard.js` (PreToolUse hook), and updated `.claude/settings.json` (hook registration).

After this plan, a user can write a TASK-XXX.md file and confirm the hook allows valid content and denies invalid transitions or `repo: both`.
</objective>

<execution_context>
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md

<interfaces>
<!-- Key patterns the executor must replicate. Extracted from codebase. -->

From .claude/hooks/gsd-workflow-guard.js (stdin/stdout/exit contract):
- Shebang: #!/usr/bin/env node
- stdin accumulation with 3-second timeout: setTimeout(() => process.exit(0), 3000)
- Wrap all logic in try/catch with process.exit(0) on error
- Advisory output: process.stdout.write(JSON.stringify(output)) with hookEventName: "PreToolUse"
- Deny output: same stdout write with permissionDecision: 'deny' and permissionDecisionReason, then process.exit(0)
- Never use process.exit(2) with JSON output

From .claude/settings.json (PreToolUse block structure, lines 61-91):
- Node path: "/Users/tarasbannyi/.nvm/versions/node/v22.18.0/bin/node"
- Command pattern: "\"<node-path>\" \"$CLAUDE_PROJECT_DIR\"/.claude/hooks/<hookname>.js"
- timeout: 5
- matcher: "Write|Edit"
- Append new entry to existing PreToolUse array (do not replace existing entries)

From .claude/hooks/gsd-workflow-guard.js (path filter pattern):
- const filePath = data.tool_input?.file_path || data.tool_input?.path || '';
- Early exit if not target path: if (!filePath.includes('.planning/work/') || !filePath.endsWith('.md')) process.exit(0);
</interfaces>
</context>

## Phase Goal

**As a** developer, **I want to** write task files that are automatically validated against the schema and status lifecycle, **so that** the pipeline has a reliable, enforced contract for task state management.

<tasks>

<task type="auto">
  <name>Task 1: Create task schema and work directory</name>
  <files>.planning/task-schema.yaml, .planning/work/.gitkeep</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md — decisions D-14 through D-18 (required fields, status values, ID format, epic directory structure)
    - .planning/phases/01-foundation/01-RESEARCH.md — "task-schema.yaml Structure" code example (exact YAML structure to follow)
    - .planning/REQUIREMENTS.md — FOUND-01 (exact field names and status values required by requirement)
  </read_first>
  <action>
Create `.planning/task-schema.yaml` documenting the full task file contract per D-14 through D-18. The file must include:

- `version: 1` at top level
- `task_id_format: "TASK-\\d{3}"` (three-digit zero-padded per D-17)
- `task_file_pattern: ".planning/work/<epic-name>/TASK-XXX.md"` (per D-18)
- `fields:` block with all nine required fields (per D-14): `id`, `title`, `status`, `priority`, `repo`, `epic`, `complexity`, `created-at`, `updated-at`
  - `id`: type string, pattern "TASK-\\d{3}", required true, example "TASK-001"
  - `title`: type string, required true
  - `status`: type enum, values [readyForDevelop, inProgress, inReview, inTesting, forTeamLeadCheck, done], required true, initial: readyForDevelop
  - `priority`: type enum, values [low, medium, high, critical], required true
  - `repo`: type enum, values [be, fe] — note that 'both' is explicitly DISALLOWED (per D-15), required true
  - `epic`: type string, required true, note "Must match the parent directory name under .planning/work/"
  - `complexity`: type integer, min 1, max 10, required true
  - `created-at`: type string, format ISO8601, required true
  - `updated-at`: type string, format ISO8601, required true, note "Auto-updated by task-state-guard.js on every valid write"
- `lifecycle:` block with six states and their allowed_next arrays matching the VALID_TRANSITIONS in the hook (per FOUND-02):
  - readyForDevelop: allowed_next [inProgress]
  - inProgress: allowed_next [inReview, readyForDevelop]
  - inReview: allowed_next [inTesting, inProgress]
  - inTesting: allowed_next [forTeamLeadCheck, inProgress]
  - forTeamLeadCheck: allowed_next [done, inProgress]
  - done: allowed_next []

Create `.planning/work/.gitkeep` as an empty placeholder file to establish the task file root directory per D-18. This directory will hold `<epic-name>/TASK-XXX.md` subdirectories.
  </action>
  <verify>
    <automated>grep -c "task_id_format" /Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/task-schema.yaml && grep -c "forTeamLeadCheck" /Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/task-schema.yaml && ls /Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/work/</automated>
  </verify>
  <acceptance_criteria>
    - .planning/task-schema.yaml exists and contains `version: 1`
    - .planning/task-schema.yaml contains all nine required field names: id, title, status, priority, repo, epic, complexity, created-at, updated-at
    - .planning/task-schema.yaml contains all six status values: readyForDevelop, inProgress, inReview, inTesting, forTeamLeadCheck, done
    - .planning/task-schema.yaml lifecycle block shows `readyForDevelop: allowed_next: [inProgress]` and `done: allowed_next: []`
    - .planning/task-schema.yaml repo field values list [be, fe] only (not 'both')
    - .planning/work/ directory exists (confirmed by ls showing .gitkeep)
  </acceptance_criteria>
  <done>task-schema.yaml fully documents FOUND-01 schema and FOUND-02 lifecycle; .planning/work/ directory exists</done>
</task>

<task type="auto">
  <name>Task 2: Create task-state-guard.js PreToolUse hook and register in settings.json</name>
  <files>.claude/hooks/task-state-guard.js, .claude/settings.json</files>
  <read_first>
    - .claude/hooks/gsd-workflow-guard.js — stdin boilerplate (lines 1-21), path filter pattern (lines 40-45), advisory output pattern (lines 78-90): replicate the exact shebang, stdin accumulation, and try/catch structure
    - .claude/settings.json — current PreToolUse array (lines 61-91): append new entry, do not replace existing entries; copy exact node path "/Users/tarasbannyi/.nvm/versions/node/v22.18.0/bin/node" from existing entries
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 2 (deny output format), Pattern 3 (frontmatter extraction regex), "Hook: Full Skeleton" code example, Pitfall 4 (path filter must be inside hook)
    - .planning/phases/01-foundation/01-PATTERNS.md — hook stdin/stdout/exit contract shared pattern, path filter pattern
    - .planning/phases/01-foundation/01-CONTEXT.md — D-09 through D-16 (hook behavior, deny semantics, updated-at injection note, new-file initial status rule)
  </read_first>
  <action>
Create `.claude/hooks/task-state-guard.js` as a PreToolUse hook. Follow the exact stdin boilerplate from gsd-workflow-guard.js (shebang `#!/usr/bin/env node`, require fs and path, let input accumulation with 3-second timeout, process.stdin.on('data'/'end'), clearTimeout, try/catch with process.exit(0) on error).

Hook logic (in order, per RESEARCH.md Pitfall 4 and 5):

1. Guard: if tool_name is not 'Write' and not 'Edit', exit 0 immediately.

2. Path filter: extract `filePath = data.tool_input?.file_path || data.tool_input?.path || ''`. If filePath does not include `.planning/work/` OR does not end with `.md`, exit 0 immediately.

3. Extract newStatus from tool call:
   - For Write: extract from `data.tool_input.content` using `extractFrontmatterField(content, 'status')`
   - For Edit: extract from `data.tool_input.new_string` using `extractFrontmatterField(new_string, 'status')`
   - If no newStatus found, exit 0 (not setting status — allow).

4. Check file existence: `fs.existsSync(filePath)`.

5. If file does NOT exist (new file creation):
   - If newStatus !== 'readyForDevelop': call deny(`New task files must have status: readyForDevelop. Got: ${newStatus}`)
   - Otherwise proceed to repo check (step 7).

6. If file DOES exist (edit):
   - Read current disk content: `const diskContent = fs.readFileSync(filePath, 'utf8')` (store this — it is reused in steps 7 and 8)
   - Extract current status: `extractFrontmatterField(diskContent, 'status')`
   - Look up `VALID_TRANSITIONS[currentStatus]` (array of allowed next states)
   - If newStatus is not in the allowed array: call deny(`Invalid status transition: ${currentStatus} -> ${newStatus}. Allowed: [${allowed.join(', ')}]`)

7. Repo check — enforced on the reconstructed final content (per D-15):
   - For Write: extract repo value from `data.tool_input.content` using `content.match(/^repo:\s*(\S+)/m)?.[1]`
   - For Edit: reconstruct the final file content by applying the edit to diskContent — `const finalContent = diskContent.replace(data.tool_input.old_string, data.tool_input.new_string)` — then extract repo from finalContent using `finalContent.match(/^repo:\s*(\S+)/m)?.[1]`
   - If repoValue === 'both': call deny('repo: both is not allowed. Split into separate be and fe tasks.')
   - Store finalContent (Edit path) or the Write content for use in step 8.

8. ALLOW path — inject updated-at into the authoritative final content (per D-16). The hook must NOT exit 0 bare. It must always return modifiedInput with the complete updated file content:
   - For Write tool calls: take `data.tool_input.content`, apply `/(updated-at:\s*)([^\n]+)/` regex replace to stamp `new Date().toISOString()`. Output `{ "hookSpecificOutput": { "hookEventName": "PreToolUse", "permissionDecision": "allow" }, "modifiedInput": { "content": <updatedContent> } }` to stdout and exit 0.
   - For Edit tool calls: take the finalContent already reconstructed in step 7 (diskContent with old_string replaced by new_string), apply the same `/(updated-at:\s*)([^\n]+)/` regex replace to stamp `new Date().toISOString()`. Output `{ "hookSpecificOutput": { "hookEventName": "PreToolUse", "permissionDecision": "allow" }, "modifiedInput": { "content": <updatedFinalContent> } }` to stdout and exit 0. The hook returns the full reconstructed file content — not new_string — so the Edit tool applies the hook's content as a complete file replacement, guaranteeing updated-at is stamped regardless of what fields the agent's new_string contained.
   - In both cases use `process.stdout.write(JSON.stringify(output))` followed by `process.exit(0)`.

The `deny(reason)` function must output JSON to stdout and exit 0:
`{ hookSpecificOutput: { hookEventName: 'PreToolUse', permissionDecision: 'deny', permissionDecisionReason: reason } }`

The `extractFrontmatterField(content, field)` function must:
- Match `^---\n([\s\S]*?)\n---` to get frontmatter block
- Search in the frontmatter block (or full content if no block found)
- Return `match[1]` from `new RegExp(`^${field}:\\s*(\\S+)`, 'm')`

VALID_TRANSITIONS must be a plain JS object (no external dependencies):
`readyForDevelop: ['inProgress'], inProgress: ['inReview', 'readyForDevelop'], inReview: ['inTesting', 'inProgress'], inTesting: ['forTeamLeadCheck', 'inProgress'], forTeamLeadCheck: ['done', 'inProgress'], done: []`

Update `.claude/settings.json`: append a new entry to the existing `PreToolUse` array. Copy the exact structure and node binary path from existing entries. The new entry:
- matcher: "Write|Edit"
- type: "command"
- command: `"/Users/tarasbannyi/.nvm/versions/node/v22.18.0/bin/node" "$CLAUDE_PROJECT_DIR"/.claude/hooks/task-state-guard.js`
- timeout: 5

Do NOT remove or modify any existing PreToolUse entries.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/hooks/task-state-guard.js','utf8'); const hookOk=src.includes('permissionDecision')&&src.includes('VALID_TRANSITIONS')&&src.includes('.planning/work/')&&src.includes('readyForDevelop')&&src.includes('modifiedInput')&&src.includes('updated-at'); const s=JSON.parse(fs.readFileSync('/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/settings.json','utf8')); const settingsOk=s.hooks.PreToolUse.some(e=>e.hooks&&e.hooks.some(h=>h.command&&h.command.includes('task-state-guard'))); console.log(hookOk&&settingsOk?'PASS':'FAIL hook='+hookOk+' settings='+settingsOk)"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/hooks/task-state-guard.js exists and contains: `permissionDecision`, `VALID_TRANSITIONS`, `.planning/work/`, `readyForDevelop`, `extractFrontmatterField`
    - .claude/hooks/task-state-guard.js contains `modifiedInput` and `updated-at` (D-16 injection path)
    - On the ALLOW path for a Write call, the hook outputs `{ hookSpecificOutput: { permissionDecision: 'allow' }, modifiedInput: { content: <content with updated-at replaced to current ISO timestamp> } }` — agents never need to set updated-at manually
    - On the ALLOW path for an Edit call, the hook always reconstructs the final file content (diskContent with old_string replaced by new_string), stamps updated-at in that reconstructed content, and returns `modifiedInput: { content: <reconstructedContent> }` — this guarantees updated-at injection even when the agent's new_string contains only the status field change (per D-16)
    - On the ALLOW path for an Edit call, the repo field is validated from the reconstructed final content (not just new_string), blocking a two-step attack where a valid Write is followed by an Edit that introduces repo: both (per D-15)
    - .claude/hooks/task-state-guard.js path-filter check exits 0 for files not under .planning/work/
    - .claude/hooks/task-state-guard.js deny function outputs JSON with hookEventName: 'PreToolUse' and permissionDecision: 'deny' and calls process.exit(0) (not exit 2)
    - .claude/settings.json PreToolUse array contains an entry with command including "task-state-guard.js"
    - .claude/settings.json existing PreToolUse entries (gsd-prompt-guard.js, gsd-read-guard.js, gsd-workflow-guard.js) are still present and unchanged
    - Running `node /Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/hooks/task-state-guard.js` with empty stdin exits 0 within 3 seconds (stdin timeout fires)
  </acceptance_criteria>
  <done>task-state-guard.js correctly validates status transitions and repo field (from reconstructed final content on Edit calls), injects current ISO timestamp into updated-at on every allowed write via modifiedInput with full file content, and is registered in settings.json as a PreToolUse hook</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → task file | Agent writes .planning/work/**/*.md; hook intercepts before write completes |
| Hook → file system | Hook reads current task file from disk to determine current state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | task-state-guard.js path filter | mitigate | Hook checks `filePath.includes('.planning/work/')` AND `.endsWith('.md')` before any validation — files outside scope exit 0 immediately |
| T-01-02 | Denial of Service | Hook stdin hang | mitigate | 3-second stdin timeout fires process.exit(0) — hook never blocks indefinitely |
| T-01-03 | Tampering | Edit tool new_string partial update | mitigate | On the Edit allow path, the hook reconstructs the full final file content (diskContent + substitution), validates repo from that reconstructed content, and returns it via modifiedInput.content — partial new_string that omits updated-at or contains repo: both is caught regardless. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero external packages. No slopcheck required. |
</threat_model>

<verification>
Manual smoke tests to run after both tasks complete:

1. Write a valid task file to `.planning/work/test-epic/TASK-001.md` with all required fields and `status: readyForDevelop` — hook must allow and the saved file must have `updated-at` set to a current ISO timestamp injected by the hook.
2. Attempt to write the same file with `status: done` (invalid initial status) — hook must deny with reason "New task files must have status: readyForDevelop".
3. After file exists with `status: readyForDevelop`, attempt to edit status to `done` directly (skipping intermediate states) — hook must deny.
4. After file exists with `status: readyForDevelop`, edit status to `inProgress` using new_string that contains ONLY the status line change (not updated-at) — hook must allow and the saved file must have a freshly injected `updated-at` timestamp (verifies D-16 fix).
5. After a valid Write, attempt an Edit where new_string introduces `repo: both` — hook must deny (verifies D-15 fix on Edit path).
6. Attempt to write a task file with `repo: both` — hook must deny with reason about splitting tasks.
7. Confirm `.claude/settings.json` still contains all original PreToolUse entries plus the new task-state-guard.js entry.
</verification>

<success_criteria>
- .planning/task-schema.yaml exists with version: 1, all 9 required fields, all 6 status values, and full lifecycle transitions
- .planning/work/ directory exists
- .claude/hooks/task-state-guard.js implements PreToolUse deny logic for invalid status transitions and repo: both
- .claude/hooks/task-state-guard.js injects current ISO timestamp into updated-at field on every allowed write via modifiedInput with full file content (per D-16) — guaranteed on both Write and Edit paths
- .claude/hooks/task-state-guard.js validates repo field from reconstructed final content on Edit calls (per D-15) — blocks two-step repo: both injection
- .claude/settings.json has task-state-guard.js registered under PreToolUse with matcher "Write|Edit" and timeout 5
- All existing PreToolUse hooks in settings.json remain intact
- FOUND-01 and FOUND-02 requirements satisfied
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
