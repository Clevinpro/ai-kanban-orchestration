---
phase: 06-kanban-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - kanban-server/client/package.json
  - kanban-server/client/vite.config.js
  - kanban-server/client/index.html
  - kanban-server/client/src/main.jsx
  - kanban-server/client/src/index.css
  - kanban-server/client/src/App.jsx
  - kanban-server/client/src/components/Board.jsx
  - kanban-server/client/src/components/TaskCard.jsx
autonomous: false
requirements:
  - KANBAN-01
  - KANBAN-02
  - KANBAN-03
must_haves:
  truths:
    - "Opening http://localhost:5173 shows the Kanban board with six labeled columns"
    - "All task cards from .planning/work/ appear in the correct column matching their status field"
    - "Each card displays: title, complexity badge, repo badge (FE/BE with color), and epic name"
    - "Editing a task file's status on disk causes the card to move to the new column within 1 second (SSE live update)"
    - "The board renders all six columns without horizontal scroll at 1440px viewport width"
    - "When kanban-server is unreachable, the board shows an error/empty state rather than crashing"
  artifacts:
    - path: "kanban-server/client/package.json"
      provides: "Vite + React + Tailwind v4 + @hello-pangea/dnd dependencies"
      contains: "\"type\": \"module\""
    - path: "kanban-server/client/vite.config.js"
      provides: "Vite config with react() plugin, tailwindcss() plugin, proxy to port 6111, outDir: ../public"
      contains: "outDir: '../public'"
    - path: "kanban-server/client/index.html"
      provides: "SPA entry HTML with <div id=\"root\"> and module script tag"
      contains: "id=\"root\""
    - path: "kanban-server/client/src/main.jsx"
      provides: "React 18 createRoot entry point"
      contains: "createRoot"
    - path: "kanban-server/client/src/index.css"
      provides: "Tailwind v4 import"
      contains: "@import \"tailwindcss\""
    - path: "kanban-server/client/src/App.jsx"
      provides: "Board state via useReducer, SSE connection via useEffect, renders Board"
      contains: "useReducer"
    - path: "kanban-server/client/src/components/Board.jsx"
      provides: "Six-column Tailwind grid layout with column headers and task lists"
      contains: "COLUMN_ORDER"
    - path: "kanban-server/client/src/components/TaskCard.jsx"
      provides: "Task card rendering title, complexity, repo badge, epic"
      contains: "task.complexity"
  key_links:
    - from: "kanban-server/client/src/App.jsx useEffect"
      to: "GET /events at port 6111"
      via: "new EventSource('/events')"
      pattern: "EventSource.*'/events'"
    - from: "kanban-server/client/src/App.jsx boardReducer SSE_UPDATE"
      to: "Board.jsx columns prop"
      via: "state object keyed by COLUMN_ORDER values"
      pattern: "SSE_UPDATE"
    - from: "kanban-server/client/vite.config.js server.proxy"
      to: "http://localhost:6111"
      via: "proxy config for /events, /tasks, /health"
      pattern: "localhost:6111"
---

<objective>
Create the complete Vite + React client inside kanban-server/client/. This is the live Kanban board — a browser SPA that connects to the existing Express server via SSE and renders all six status columns with live-updating task cards.

The plan delivers a working board at http://localhost:5173 (dev) where: task cards load from the SSE initial snapshot, live file changes update cards in real-time, each card shows all four required fields (title, complexity, repo badge, epic), and all six columns are visible without scroll at 1440px.

Drag-and-drop wiring (PATCH calls) is handled in Plan 06-03. This plan creates the Board and TaskCard components without @hello-pangea/dnd — that wiring is layered on in Wave 3. The components must be structured so Plan 06-03 can wrap them with DragDropContext/Droppable/Draggable without restructuring.

Purpose: KANBAN-01 (six columns), KANBAN-02 (SSE live updates), KANBAN-03 (card fields).

Output: 8 new files under kanban-server/client/. No modifications to kanban-server/index.js or package.json (handled by Plan 06-01).
</objective>

<execution_context>
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/ROADMAP.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/STATE.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/phases/06-kanban-ui/06-CONTEXT.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/phases/06-kanban-ui/06-RESEARCH.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/phases/06-kanban-ui/06-PATTERNS.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/phases/06-kanban-ui/06-01-SUMMARY.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/kanban-server/package.json

<interfaces>
<!-- SSE event payload shape from kanban-server/index.js pushEvent() — line 109 -->
<!-- Client receives these objects via task-updated SSE events -->
Task object shape:
{
  id: "TASK-001",           // string — unique, used as React key
  title: "...",             // string — primary card text
  status: "inProgress",    // enum: readyForDevelop|inProgress|inReview|inTesting|forTeamLeadCheck|done|stopped
  priority: "high",         // enum: low|medium|high|critical
  repo: "be",              // enum: "be"|"fe"
  epic: "pipeline-integration",  // string
  complexity: 5,            // integer 1-10
  "created-at": "...",
  "updated-at": "...",
  deleted: true            // only on delete events
}

COLUMN_ORDER constant (use in both App.jsx and Board.jsx — extract to shared constant or duplicate):
['readyForDevelop', 'inProgress', 'inReview', 'inTesting', 'forTeamLeadCheck', 'done']

COLUMN_LABELS mapping (Claude's discretion — from RESEARCH.md Code Examples):
{
  readyForDevelop:  'Ready',
  inProgress:       'In Progress',
  inReview:         'In Review',
  inTesting:        'Testing',
  forTeamLeadCheck: 'TL Check',
  done:             'Done',
}

boardReducer actions (App.jsx — from RESEARCH.md Pattern 4):
- SSE_UPDATE: { type: 'SSE_UPDATE', task } — place task in correct column (or remove if task.deleted)
- DRAG_OPTIMISTIC: { type: 'DRAG_OPTIMISTIC', taskId, newStatus } — FULL implementation required here (Plan 06-03 only adds dispatch calls, not switch cases)
- DRAG_REVERT: { type: 'DRAG_REVERT', taskId, originalStatus } — FULL implementation required here (same reason)
Both DRAG actions MUST have full implementations in this plan's reducer — NOT stubs or no-ops. Plan 06-03 will call dispatch({type:'DRAG_OPTIMISTIC',...}) and dispatch({type:'DRAG_REVERT',...}) but will NOT add switch cases. Implement them now.

<!-- Tailwind v4 setup — NO tailwind.config.js, NO postcss.config.js -->
<!-- D-10 in CONTEXT.md describes a Tailwind v3 setup (tailwind.config.js, postcss.config.js). RESEARCH.md confirms v4 replaces that entirely with @tailwindcss/vite. Do NOT create tailwind.config.js or postcss.config.js. -->
<!-- Column.jsx from PATTERNS.md is NOT a separate file in this plan — column rendering is inlined in Board.jsx for MVP simplicity. PATTERNS.md's Column.jsx section describes the Droppable pattern to apply inside Board.jsx's column map in Plan 06-03. -->
vite.config.js plugins: [react(), tailwindcss()]  // tailwindcss from @tailwindcss/vite
index.css: @import "tailwindcss";                 // single line, replaces v3 directives

<!-- Vite proxy — use relative URLs in React components -->
All fetch/EventSource calls use relative paths (e.g. /events, /tasks/:id/status)
so the same code works in dev (proxied via Vite) and production (served by Express directly).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify package legitimacy before npm install</name>
  <what-built>
    Nothing yet — this checkpoint gates the npm install in Task 2.
    RESEARCH.md Package Legitimacy Audit flagged all packages as [ASSUMED] because slopcheck was unavailable.
    The following packages will be installed inside kanban-server/client/:
    - vite@^8.0.14 (vitejs/vite — official, high downloads)
    - @vitejs/plugin-react@^6.0.2 (vitejs/vite-plugin-react — official Vite org)
    - react@^19.2.6 (facebook/react — official)
    - react-dom@^19.2.6 (facebook/react — official)
    - @hello-pangea/dnd@^18.0.1 (hello-pangea/dnd — maintained react-beautiful-dnd fork)
    - tailwindcss@^4.3.0 (tailwindlabs/tailwindcss — official)
    - @tailwindcss/vite@^4.3.0 (tailwindlabs/tailwindcss — official Tailwind org)
  </what-built>
  <how-to-verify>
    For each package, spot-check on npmjs.com:
    1. https://www.npmjs.com/package/vite — confirm downloads + publisher
    2. https://www.npmjs.com/package/@vitejs/plugin-react — confirm downloads + publisher
    3. https://www.npmjs.com/package/react — confirm downloads + publisher
    4. https://www.npmjs.com/package/@hello-pangea/dnd — confirm downloads + source repo
    5. https://www.npmjs.com/package/tailwindcss — confirm downloads + publisher
    6. https://www.npmjs.com/package/@tailwindcss/vite — confirm downloads + publisher

    All six are well-known packages with millions of weekly downloads from official publishers.
    This is a local dev tool — risk is minimal for these industry-standard packages.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with npm install, or describe any package concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create all 8 client source files and run npm install</name>
  <files>
    kanban-server/client/package.json,
    kanban-server/client/vite.config.js,
    kanban-server/client/index.html,
    kanban-server/client/src/main.jsx,
    kanban-server/client/src/index.css,
    kanban-server/client/src/App.jsx,
    kanban-server/client/src/components/Board.jsx,
    kanban-server/client/src/components/TaskCard.jsx
  </files>
  <read_first>
    - .planning/phases/06-kanban-ui/06-RESEARCH.md (Pattern 1 vite.config, Pattern 2 useSSE, Pattern 4 boardReducer, Pattern 7 client package.json, Code Examples section)
    - .planning/phases/06-kanban-ui/06-PATTERNS.md (all sections — contains exact code patterns for every file)
    - .planning/phases/06-kanban-ui/06-CONTEXT.md (D-01 through D-11 decisions — all locked)
    - .planning/phases/06-kanban-ui/06-01-SUMMARY.md (confirm Plan 06-01 completed successfully)
    - kanban-server/package.json (confirm type: commonjs — client/ needs its own package.json with type: module)
  </read_first>
  <action>
Create the kanban-server/client/ directory tree with exactly these 8 files. Follow the patterns from RESEARCH.md and PATTERNS.md precisely — do not invent alternatives.

FILE 1: kanban-server/client/package.json
Must declare `"type": "module"` (required — overrides parent kanban-server/package.json which declares `"type": "commonjs"`; without this, Vite ESM imports fail with SyntaxError per RESEARCH.md Pitfall 2).
Name: "kanban-client", private: true.
Dependencies: react@^19.2.6, react-dom@^19.2.6, @hello-pangea/dnd@^18.0.1
DevDependencies: vite@^8.0.14, @vitejs/plugin-react@^6.0.2, tailwindcss@^4.3.0, @tailwindcss/vite@^4.3.0
Scripts: dev, build, preview (standard Vite commands).
Do NOT add postcss or autoprefixer — not needed with @tailwindcss/vite (RESEARCH.md anti-pattern warning).

FILE 2: kanban-server/client/vite.config.js
Import defineConfig from 'vite', react from '@vitejs/plugin-react', tailwindcss from '@tailwindcss/vite'.
Plugins: [react(), tailwindcss()].
Build config: outDir: '../public', emptyOutDir: true (emptyOutDir required because ../public is outside client/ root — RESEARCH.md Pitfall 6).
Server config: port: 5173, proxy entries for /events (with changeOrigin: true and configure hook setting Connection: keep-alive on proxyReq), /tasks (changeOrigin: true), /health (changeOrigin: true).
Also add /stop proxy entry: target http://localhost:6111, changeOrigin: true (for POST /tasks/:id/stop).

FILE 3: kanban-server/client/index.html
Standard Vite SPA entry. lang="en", charset="utf-8", title="Kanban", viewport meta tag.
Body: `<div id="root"></div>` followed by `<script type="module" src="/src/main.jsx"></script>`.
No base href tag (not needed for single-page app with no sub-routes).

FILE 4: kanban-server/client/src/main.jsx
Import StrictMode from 'react', createRoot from 'react-dom/client', App from './App', './index.css'.
Use createRoot(document.getElementById('root')).render(<StrictMode><App /></StrictMode>).
No TypeScript casts, no providers (no router, no query client).

FILE 5: kanban-server/client/src/index.css
Single line: `@import "tailwindcss";`
No tailwind.config.js, no @tailwind base/components/utilities directives — Tailwind v4 CSS-first approach.

FILE 6: kanban-server/client/src/App.jsx
Declare COLUMN_ORDER array at module scope: ['readyForDevelop', 'inProgress', 'inReview', 'inTesting', 'forTeamLeadCheck', 'done'].
Declare initialState as Object.fromEntries(COLUMN_ORDER.map(col => [col, []])).
Implement boardReducer(state, action) with three switch cases: SSE_UPDATE (remove task from all columns, add to correct column based on task.status — skip if status not in COLUMN_ORDER; handle task.deleted by removing from all columns), DRAG_OPTIMISTIC (remove task from all columns, add to action.newStatus column — full implementation, not a stub), DRAG_REVERT (remove task from all columns, re-add to action.originalStatus column — full implementation, not a stub; equivalent to SSE_UPDATE with originalStatus), default returns state. All three cases must have full implementations here.
In the App component: call useReducer(boardReducer, initialState). Use useEffect to open EventSource('/events'), add 'task-updated' event listener that JSON.parses e.data and dispatches SSE_UPDATE, set onerror to close the EventSource and setTimeout(connect, 3000) for reconnect, return cleanup closing EventSource and clearing the timeout. Use a stable connect() inner function pattern so reconnect and initial connect use the same code (RESEARCH.md Pattern 2). Pass dispatch as a stable ref to prevent useEffect re-running (useReducer dispatch is stable — safe as dependency).
App render: return `<Board tasks={state} dispatch={dispatch} />`.
Export App as default.

FILE 7: kanban-server/client/src/components/Board.jsx
Import COLUMN_ORDER at top (declare it as a module-level constant here — same array as in App.jsx; no shared constants file needed for MVP).
Declare COLUMN_LABELS object mapping status values to display strings: readyForDevelop → 'Ready', inProgress → 'In Progress', inReview → 'In Review', inTesting → 'Testing', forTeamLeadCheck → 'TL Check', done → 'Done'.
Board component receives props: tasks (object keyed by status), dispatch.
Render: outer div with Tailwind classes `flex gap-2 p-3 min-h-screen bg-gray-50` to create the six-column layout. For each status in COLUMN_ORDER, render a column div with classes `flex-1 min-w-0 flex flex-col bg-gray-100 rounded-lg p-2` containing:
  - A column header: `<h2 className="text-xs font-semibold uppercase tracking-wider text-gray-500 mb-2 px-1 flex items-center gap-1">{COLUMN_LABELS[status]}<span className="text-gray-400 font-normal">{tasks[status]?.length ?? 0}</span></h2>`
  - A task list: map tasks[status] || [] to TaskCard components with key={task.id} and task={task}.
D-11: compact density — all 6 columns must be visible at 1440px. flex-1 on each column ensures equal width distribution.
Plan 06-03 will wrap this with DragDropContext and replace the inner column div with Droppable — structure the column div so it can accept a ref prop and spread droppableProps without a refactor (i.e., it should be a plain div, not a custom component wrapping the Droppable content).
Export Board as default.

FILE 8: kanban-server/client/src/components/TaskCard.jsx
Receives props: task.
Declare repoBadge(repo) helper: returns 'bg-blue-100 text-blue-700' for 'be', 'bg-purple-100 text-purple-700' for 'fe', 'bg-gray-100 text-gray-700' fallback.
Render a div with classes `bg-white rounded-md shadow-sm p-2 mb-1.5 text-xs cursor-grab` containing:
  - Task title: `<div className="font-medium text-gray-800 leading-snug mb-1 truncate">{task.title}</div>`
  - Badge row: `<div className="flex items-center gap-1 flex-wrap">` containing:
    - Repo badge: `<span className={\`px-1.5 py-0.5 rounded text-[10px] font-semibold \${repoBadge(task.repo)}\`}>{task.repo.toUpperCase()}</span>`
    - Epic: `<span className="text-gray-500 truncate flex-1">{task.epic}</span>`
    - Complexity: `<span className="ml-auto text-gray-400 font-mono">{task.complexity}</span>`
Plan 06-03 will wrap this div with Draggable — the outer div of TaskCard must accept forwarded ref and prop spreads. Use React.forwardRef on TaskCard so Plan 06-03 can pass provided.innerRef to it: `const TaskCard = React.forwardRef(({ task }, ref) => <div ref={ref} ...>)`. This avoids a refactor in Plan 06-03.
Export TaskCard as default.

AFTER creating all 8 files, run:
`cd kanban-server/client && npm install`
This installs all dependencies listed in client/package.json. Expected output: node_modules created, package-lock.json created. If install fails, report the error verbatim.

IMPORTANT: Do not run npm install from the kanban-server/ root — it would install client deps into the wrong node_modules and mix ESM/CJS packages.
  </action>
  <verify>
    <automated>
      cd /Users/tarasbannyi/TestAI/ai-agent-microservices && grep -c '"type": "module"' kanban-server/client/package.json && grep -c "outDir: '../public'" kanban-server/client/vite.config.js && grep -c 'id="root"' kanban-server/client/index.html && grep -c 'createRoot' kanban-server/client/src/main.jsx && grep -c '@import "tailwindcss"' kanban-server/client/src/index.css && grep -c 'useReducer' kanban-server/client/src/App.jsx && grep -c 'COLUMN_ORDER' kanban-server/client/src/components/Board.jsx && grep -c 'task.complexity' kanban-server/client/src/components/TaskCard.jsx && ls kanban-server/client/node_modules/.bin/vite
    </automated>
  </verify>
  <acceptance_criteria>
    - kanban-server/client/package.json contains `"type": "module"` (grep -c returns 1)
    - kanban-server/client/vite.config.js contains `outDir: '../public'` (grep -c returns 1)
    - kanban-server/client/vite.config.js contains `localhost:6111` (proxy target, grep -c returns at least 1)
    - kanban-server/client/index.html contains `id="root"` (grep -c returns 1)
    - kanban-server/client/index.html contains `src="/src/main.jsx"` (grep -c returns 1)
    - kanban-server/client/src/main.jsx contains `createRoot` (grep -c returns 1)
    - kanban-server/client/src/index.css contains `@import "tailwindcss"` (grep -c returns 1)
    - kanban-server/client/src/App.jsx contains `useReducer` (grep -c returns 1)
    - kanban-server/client/src/App.jsx contains `SSE_UPDATE` (grep -c returns 1)
    - kanban-server/client/src/App.jsx contains `DRAG_OPTIMISTIC` (grep -c returns at least 1)
    - kanban-server/client/src/App.jsx contains `DRAG_REVERT` (grep -c returns at least 1)
    - kanban-server/client/src/App.jsx contains `EventSource('/events')` (grep -c returns 1)
    - kanban-server/client/src/components/Board.jsx contains `COLUMN_ORDER` (grep -c returns at least 2)
    - kanban-server/client/src/components/Board.jsx contains `COLUMN_LABELS` (grep -c returns at least 1)
    - kanban-server/client/src/components/TaskCard.jsx contains `task.complexity` (grep -c returns 1)
    - kanban-server/client/src/components/TaskCard.jsx contains `repoBadge` (grep -c returns 1)
    - kanban-server/client/node_modules/.bin/vite exists (npm install completed)
    - `cd kanban-server/client && npm run build` exits 0 and creates kanban-server/public/index.html
  </acceptance_criteria>
  <done>All 8 client source files exist, npm install completed, npm run build exits 0 and produces kanban-server/public/index.html.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke test live board at http://localhost:5173</name>
  <what-built>
    Complete Vite + React Kanban board serving at port 5173 (dev) with:
    - Six status columns: Ready, In Progress, In Review, Testing, TL Check, Done
    - SSE connection to kanban-server at port 6111 loading all existing task cards
    - Each card showing title, complexity badge, repo badge (FE=purple, BE=blue), and epic name
    - Live updates: editing a task file's status moves the card to the new column
  </what-built>
  <how-to-verify>
    1. Start the kanban-server: `node kanban-server/index.js` (in a separate terminal — leave running)
    2. Start the Vite dev server: `cd kanban-server && npm run dev`
    3. Open http://localhost:5173 in a browser
    4. Verify: six column headers are visible (Ready, In Progress, In Review, Testing, TL Check, Done) without horizontal scroll
    5. Verify: task cards appear in the correct columns based on current task file status values
    6. Verify: each card shows title, repo badge (blue BE / purple FE), epic name, and complexity number
    7. Live update test: edit the status field in any task file under .planning/work/ (e.g. change inProgress to inReview), save — verify the card moves to the new column within 1 second without a page refresh
    8. Viewport check: at 1440px browser width, all 6 column headers are visible without horizontal scrollbar
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 checks pass. Describe any failures so executor can fix before proceeding to Plan 06-03.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ← SSE from Express | task-updated events received and parsed via JSON.parse(e.data) |
| Vite dev server → Express proxy | /events, /tasks, /health forwarded to localhost:6111 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | SSE JSON.parse in App.jsx | accept | Local dev tool — data is internal task files; no sensitive data in transit; no authentication needed |
| T-06-05 | Denial of Service | EventSource reconnect loop | mitigate | 3s reconnect delay (setTimeout 3000) prevents rapid hammering on server error; cleanup on component unmount closes EventSource and clears timeout |
| T-06-06 | Tampering | npm package install | mitigate | checkpoint:human-verify Task 1 gates install — user verifies all 7 packages against npmjs.com before install proceeds |
| T-06-SC | Tampering | npm install in client/ | mitigate | checkpoint:human-verify (Task 1) blocks install until user approves; [ASSUMED] packages from well-known official publishers |
</threat_model>

<verification>
After all tasks complete (including human-verify checkpoint approval):

1. `ls kanban-server/client/node_modules/.bin/vite` — exits 0
2. `cd kanban-server/client && npm run build` — exits 0, no errors
3. `ls kanban-server/public/index.html` — file exists (Vite build output)
4. `grep -c '"type": "module"' kanban-server/client/package.json` — returns 1
5. `grep -c "outDir: '../public'" kanban-server/client/vite.config.js` — returns 1
6. Human smoke test at http://localhost:5173 approved (Task 3 checkpoint)
</verification>

<success_criteria>
- 8 source files exist under kanban-server/client/ with correct content
- npm install completed: kanban-server/client/node_modules/ exists with vite binary
- npm run build exits 0 and produces kanban-server/public/index.html
- Human-verified: six columns visible at 1440px, cards load from SSE, live updates work, card fields match KANBAN-03
</success_criteria>

<output>
Create `.planning/phases/06-kanban-ui/06-02-SUMMARY.md` when done.
</output>
