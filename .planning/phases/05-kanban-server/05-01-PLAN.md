---
phase: 05-kanban-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/test-kanban-guard.sh
  - .claude/hooks/task-state-guard.js
  - .planning/task-schema.yaml
autonomous: true
requirements:
  - KANBAN-05

must_haves:
  truths:
    - "task-state-guard.js allows status: stopped from inProgress, inReview, inTesting, forTeamLeadCheck"
    - "task-state-guard.js denies status: stopped from readyForDevelop and done"
    - "task-schema.yaml includes stopped in its status enum and lifecycle section"
    - "scripts/test-kanban-guard.sh runs without error and all test cases pass"
  artifacts:
    - path: "scripts/test-kanban-guard.sh"
      provides: "Automated stopped-transition unit tests"
    - path: ".claude/hooks/task-state-guard.js"
      provides: "Hook with stopped as valid destination from active statuses (D-02)"
    - path: ".planning/task-schema.yaml"
      provides: "Schema with stopped in status enum and lifecycle"
  key_links:
    - from: ".claude/hooks/task-state-guard.js"
      to: "VALID_TRANSITIONS map"
      via: "stopped entries in inProgress, inReview, inTesting, forTeamLeadCheck arrays"
      pattern: "'stopped'"
    - from: "scripts/test-kanban-guard.sh"
      to: ".claude/hooks/task-state-guard.js"
      via: "printf JSON | node $HOOK"
      pattern: "permissionDecision.*allow"
---

<objective>
Extend the task-state-guard.js PreToolUse hook to accept `stopped` as a valid destination from all active statuses, add `stopped` to the task schema, and create the test script that validates these transitions. This Wave 1 plan delivers the guard-layer vertical slice: the stopped signal is validated end-to-end before the server is built.

Purpose: The kanban server's stop endpoint writes `status: stopped` to task files. The hook must allow this transition from any active status (D-02). Without this extension the server endpoint would write files that contradict the schema and the test script would fail.

Output: scripts/test-kanban-guard.sh (Wave 0 test gap closed), updated task-state-guard.js VALID_TRANSITIONS, updated task-schema.yaml status enum.
</objective>

<execution_context>
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/PROJECT.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/ROADMAP.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.planning/STATE.md

<interfaces>
From .claude/hooks/task-state-guard.js lines 10-17 (VALID_TRANSITIONS — current state):
```javascript
const VALID_TRANSITIONS = {
  readyForDevelop: ['inProgress'],
  inProgress: ['inReview', 'readyForDevelop'],
  inReview: ['inTesting', 'inProgress'],
  inTesting: ['forTeamLeadCheck', 'inProgress'],
  forTeamLeadCheck: ['done', 'inProgress'],
  done: [],
};
```

From .planning/task-schema.yaml lines 18-23 (status field — current state):
```yaml
  status:
    type: enum
    values: [readyForDevelop, inProgress, inReview, inTesting, forTeamLeadCheck, done]
    required: true
    initial: readyForDevelop
```

From scripts/test-pipeline-guard.sh (test script pattern to follow):
- set -e at top
- HOOK path via dirname + relative path
- TMPDIR=$(mktemp -d) + FIXTURE_DIR inside .planning/work/ for path filter to pass
- trap 'rm -rf "$TMPDIR"' EXIT for cleanup
- run_hook() helper: printf JSON | node $HOOK
- grep -q '"permissionDecision":"allow"' / '"permissionDecision":"deny"' assertions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scripts/test-kanban-guard.sh — stopped transition tests</name>
  <files>scripts/test-kanban-guard.sh</files>
  <read_first>
    - scripts/test-pipeline-guard.sh — copy the fixture/helper/assert structure exactly; this is the analog
    - .claude/hooks/task-state-guard.js — understand current VALID_TRANSITIONS before writing expected behavior
  </read_first>
  <behavior>
    - Case A: Write inProgress → stopped (should ALLOW)
    - Case B: Write inReview → stopped (should ALLOW)
    - Case C: Write inTesting → stopped (should ALLOW)
    - Case D: Write forTeamLeadCheck → stopped (should ALLOW)
    - Case E: Write readyForDevelop → stopped (should DENY)
    - Case F: Write done → stopped (should DENY)
    - Server smoke: node kanban-server/index.js starts with PORT=6111 and exits cleanly (placeholder — server not built yet, mark as "SKIP — server not built" and proceed without failing the script)
  </behavior>
  <action>
    Create scripts/test-kanban-guard.sh following the exact structure of scripts/test-pipeline-guard.sh.

    File must be executable (chmod +x) and start with #!/usr/bin/env bash and set -e.

    HOOK path: HOOK="$(dirname "$0")/../.claude/hooks/task-state-guard.js"

    TMPDIR setup: TMPDIR="$(mktemp -d)", FIXTURE_DIR="$TMPDIR/.planning/work/test-kanban", mkdir -p "$FIXTURE_DIR", trap 'rm -rf "$TMPDIR"' EXIT.

    Create six fixture .md files inside $FIXTURE_DIR, one per test case. Each fixture uses full valid YAML frontmatter (id: TASK-001, title, priority: medium, repo: be, epic: test-kanban, complexity: 1, created-at, updated-at) with the appropriate status value.

    run_hook() helper: printf a JSON payload with tool_name Edit, file_path, old_string "status: OLDSTATUS", new_string "status: stopped", pipe to node "$HOOK".

    Cases A-D: assert '"permissionDecision":"allow"' in output. Cases E-F: assert '"permissionDecision":"deny"' in output.

    NOTE: Cases A-D will FAIL (deny instead of allow) until Task 2 in this plan extends VALID_TRANSITIONS. This is the RED state — the test script is written first to confirm the current hook denies these, then Task 2 makes them pass (GREEN).

    After all six cases, print "All stopped-transition tests passed."

    Do NOT include a server smoke test block in this script — that belongs in a separate smoke test step once the server exists.
  </action>
  <verify>
    <automated>bash scripts/test-kanban-guard.sh 2>&1 | grep -c "PASS\|FAIL\|stopped-transition"</automated>
  </verify>
  <done>scripts/test-kanban-guard.sh exists, is executable, and runs (even if some cases currently FAIL because VALID_TRANSITIONS has not been extended yet). All six test case labels print. The script does not crash with a bash syntax error.</done>
</task>

<task type="auto">
  <name>Task 2: Extend VALID_TRANSITIONS in task-state-guard.js and update task-schema.yaml</name>
  <files>.claude/hooks/task-state-guard.js, .planning/task-schema.yaml</files>
  <read_first>
    - .claude/hooks/task-state-guard.js — read the full current file; only edit the VALID_TRANSITIONS object (lines 10-17) and nothing else
    - .planning/task-schema.yaml — read the full current file; only edit the status values list and lifecycle section
    - .planning/phases/05-kanban-server/05-CONTEXT.md — D-02 constraint for which statuses get stopped added
    - .planning/phases/05-kanban-server/05-PATTERNS.md — exact target state for VALID_TRANSITIONS and task-schema.yaml
  </read_first>
  <action>
    Edit .claude/hooks/task-state-guard.js: replace the VALID_TRANSITIONS object with the following target state (per D-02). Add 'stopped' to the allowed arrays for inProgress, inReview, inTesting, and forTeamLeadCheck. Add a stopped: [] terminal entry. Do NOT add stopped to readyForDevelop or done. Do NOT change any other line in the file.

    Target VALID_TRANSITIONS:
    - readyForDevelop: ['inProgress']
    - inProgress: ['inReview', 'readyForDevelop', 'stopped']
    - inReview: ['inTesting', 'inProgress', 'stopped']
    - inTesting: ['forTeamLeadCheck', 'inProgress', 'stopped']
    - forTeamLeadCheck: ['done', 'inProgress', 'stopped']
    - done: []
    - stopped: []

    Edit .planning/task-schema.yaml: add stopped to the values array in the status field definition so it reads: values: [readyForDevelop, inProgress, inReview, inTesting, forTeamLeadCheck, done, stopped]

    Also add a stopped lifecycle entry after the done entry:
    ```
      stopped:
        allowed_next: []
        notes:
          description: "Terminal status set by kanban-server stop-and-commit endpoint. Pipeline halts without advancing."
    ```

    Verify the changes make the test script green before completing.
  </action>
  <verify>
    <automated>bash scripts/test-kanban-guard.sh</automated>
  </verify>
  <done>
    - bash scripts/test-kanban-guard.sh exits 0 with "All stopped-transition tests passed."
    - .claude/hooks/task-state-guard.js VALID_TRANSITIONS contains 'stopped' in inProgress, inReview, inTesting, forTeamLeadCheck arrays
    - .planning/task-schema.yaml status values list includes stopped
    - .planning/task-schema.yaml has a stopped lifecycle entry with allowed_next: []
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook stdin → hook logic | JSON payload from Claude Code tool call; must not be trusted for arbitrary status values |
| task file disk → hook | File content read from disk as source of truth for current status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | VALID_TRANSITIONS extension | mitigate | stopped added only to active statuses per D-02; readyForDevelop and done excluded; tests verify both allow and deny cases |
| T-05-02 | Tampering | task-schema.yaml status enum | accept | schema is documentation/reference only; enforcement is in task-state-guard.js; no runtime injection risk |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan; only editing existing JS and YAML files |
</threat_model>

<verification>
Run after both tasks complete:

```bash
bash scripts/test-kanban-guard.sh
```

Expected: exits 0, prints "All stopped-transition tests passed."

Also verify hook denies are correct:
```bash
grep "'stopped'" .claude/hooks/task-state-guard.js | wc -l
```
Expected: 5 occurrences (one per active status array plus the stopped: [] terminal entry).
</verification>

<success_criteria>
1. scripts/test-kanban-guard.sh runs to completion with all 6 cases passing (A/B/C/D: allow; E/F: deny)
2. .claude/hooks/task-state-guard.js VALID_TRANSITIONS includes 'stopped' in inProgress, inReview, inTesting, forTeamLeadCheck and has a stopped: [] terminal entry
3. .planning/task-schema.yaml status values includes stopped and has a stopped lifecycle entry
</success_criteria>

<output>
Create .planning/phases/05-kanban-server/05-01-SUMMARY.md when done
</output>
