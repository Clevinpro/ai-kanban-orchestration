---
phase: 02-teamlead-skills
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/team-lead/execute.md
autonomous: true
requirements: [TL-02, PIPE-05]

must_haves:
  truths:
    - "Running /team-lead:execute TASK-001 reads the task file and advances status through all six stages: readyForDevelop → inProgress → inReview → inTesting → forTeamLeadCheck → done"
    - "Each stage transition is written as a single Edit call to the task file's status field"
    - "Each stage logs a one-line stub receipt: '[<Stage>] stub — Phase 3 plugs in real agent'"
    - "Each stage prints a progress trail line: '[<Stage>] Done ✓' or '[<Stage>] APPROVED ✓'"
    - "On any simulated failure, Claude pauses and prompts: 'Stage failed: [reason]. Retry / Skip / Abort?'"
    - "task-state-guard.js PreToolUse hook validates every status transition automatically (PIPE-05 already complete)"
    - "Execute command normalizes partial TASK IDs (TASK-1 or 1) to three-digit format (TASK-001)"
  artifacts:
    - path: ".claude/commands/team-lead/execute.md"
      provides: "Full execute command implementation"
      contains: "$ARGUMENTS"
    - path: ".claude/commands/team-lead/execute.md"
      provides: "Pipeline stage loop with status transitions"
      contains: "inProgress"
    - path: ".claude/commands/team-lead/execute.md"
      provides: "Failure gate with user prompt"
      contains: "Retry / Skip / Abort"
  key_links:
    - from: "/team-lead:execute invocation"
      to: ".claude/commands/team-lead/execute.md"
      via: "Claude Code slash command dispatch"
      pattern: "\\$ARGUMENTS"
    - from: "execute.md body"
      to: ".planning/work/<epic>/TASK-NNN.md"
      via: "Edit tool — one status field write per stage"
      pattern: "status:"
    - from: "Edit call on task file"
      to: ".claude/hooks/task-state-guard.js"
      via: "PreToolUse hook (automatic interception)"
      pattern: "task-state-guard"
---

<objective>
Replace the execute.md stub with a full pipeline orchestration implementation. The command reads a TASK-XXX.md file, advances its status through all six lifecycle states one at a time, logs stubbed agent receipts at each stage, prints a progress trail, and pauses for user input on any failure. Confirms PIPE-05 is satisfied by the existing task-state-guard.js.

Purpose: Gives the TeamLead the ability to drive any task through the full Developer → CodeReview → QA → TeamLeadCheck → Done pipeline in one command — with Phase 3 real agents as drop-in replacements for the stubs.

Output: `.claude/commands/team-lead/execute.md` (complete replacement of stub).
</objective>

<execution_context>
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-teamlead-skills/02-CONTEXT.md
@.planning/phases/02-teamlead-skills/02-RESEARCH.md
@.planning/phases/02-teamlead-skills/02-PATTERNS.md
</context>

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From .claude/commands/team-lead/execute.md (current stub — keep frontmatter exactly, replace body):
  name: team-lead:execute
  description: Run the full automated pipeline (Developer → CodeReview → QA → TeamLeadCheck → Done) for a single task.
  argument-hint: "<TASK-ID>"
  allowed-tools: Read, Write, Bash, Glob, Grep, Agent

From .claude/hooks/task-state-guard.js — valid status transition table:
  readyForDevelop → inProgress
  inProgress → inReview (or back to readyForDevelop on rejection)
  inReview → inTesting (or back to inProgress on CR rejection)
  inTesting → forTeamLeadCheck (or back to inProgress on QA failure)
  forTeamLeadCheck → done (or back to inProgress on TeamLeadCheck rejection)

Pipeline stage → status write mapping (per D-08 and 02-PATTERNS.md):
  Stage Developer:       readyForDevelop → inProgress
  Stage CodeReview:      inProgress → inReview
  Stage QA:              inReview → inTesting
  Stage TeamLeadCheck:   inTesting → forTeamLeadCheck
  Stage Done:            forTeamLeadCheck → done
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write full execute.md instruction body</name>
  <files>.claude/commands/team-lead/execute.md</files>
  <read_first>
    - .claude/commands/team-lead/execute.md — read current stub to confirm frontmatter fields (keep exactly as-is)
    - .claude/hooks/task-state-guard.js — VALID_TRANSITIONS map and the status field update mechanism (Edit tool intercept)
    - .planning/task-schema.yaml — full 6-state lifecycle and allowed_next arrays
    - .planning/phases/02-teamlead-skills/02-PATTERNS.md — execute.md pattern section (task ID normalization, pipeline stage loop, failure gate patterns)
    - .planning/phases/02-teamlead-skills/02-RESEARCH.md — Pattern 5 (status transition in execute command), Pitfall 5 (TASK-ID padding), Pitfall 6 (status transition race — one transition at a time)
  </read_first>
  <action>
    Replace the entire body of .claude/commands/team-lead/execute.md (everything after the closing ---).
    Keep all existing frontmatter fields exactly as-is (name, description, argument-hint, allowed-tools).

    The body must implement these steps in order (prose instructions for Claude to follow):

    CONSTRAINTS section (at the top of the body, before the steps):
    - Each stage writes ONE status transition at a time using the Edit tool. Never write two status values in a single Edit call.
    - The PreToolUse hook (task-state-guard.js) validates every transition automatically. If a transition is invalid, the hook denies the write and returns the reason.
    - Sub-agent calls in Phase 2 are stubs — log one-line receipts instead of spawning real agents.
    - Never retry automatically — always pause for user input on failure.

    STEP 1 — Normalize Task ID:
    Take $ARGUMENTS. Normalize to three-digit format: if the input is "1", "01", or "TASK-1" normalize to "TASK-001". If the input is already "TASK-001" use as-is. Use Glob on .planning/work/**/<normalized-id>.md to find the task file. If no file is found, output an error showing the normalized ID that was searched and stop.

    STEP 2 — Read task file:
    Read the task file located by Glob. Extract: title, epic, repo, status, complexity from the frontmatter. Confirm the current status is readyForDevelop before proceeding. If the status is not readyForDevelop, print a warning showing the current status and ask the user if they want to continue from the current stage (yes) or abort (no).

    STEP 3 — Execute pipeline:
    Advance through the pipeline ONE STAGE AT A TIME. For each stage, perform these three actions in sequence:
    (a) Write only the status: line in the task file frontmatter to the next status value using the Edit tool. The task-state-guard.js PreToolUse hook validates the transition and injects the updated updated-at timestamp automatically.
    (b) Log a one-line stub receipt: "[<Stage>] stub — Phase 3 plugs in real agent"
    (c) Print the progress trail line: "[<Stage>] Done ✓"

    Stage sequence and status writes (one Edit per stage — match exactly):
    | Stage | Edit: old status | Edit: new status |
    |-------|-----------------|-----------------|
    | Developer | readyForDevelop | inProgress |
    | CodeReview | inProgress | inReview |
    | QA | inReview | inTesting |
    | TeamLeadCheck | inTesting | forTeamLeadCheck |
    | Done | forTeamLeadCheck | done |

    After the final Done stage, print: "Pipeline complete. Task <id> is now done."

    STEP 4 — Failure handling:
    If at any stage a failure occurs (in Phase 2 this means the hook denied the transition, or the user signals a failure), pause immediately. Print:
    "Stage failed: [<reason>]. Retry / Skip / Abort?"
    - Retry: re-attempt the same stage status write from the beginning of that stage.
    - Skip: skip the current stage, do not write its status transition, continue to the next stage.
    - Abort: stop the pipeline. Leave the task file at its current status. Print "Pipeline aborted at [<Stage>]."
    Never automatically retry — always wait for the user's explicit choice.

    PIPE-05 note (include as a brief inline note after the constraints):
    "Status transition validation is enforced automatically by .claude/hooks/task-state-guard.js (PreToolUse hook, installed in Phase 1). No additional hook is needed for PIPE-05."
  </action>
  <verify>
    <automated>grep -c '\$ARGUMENTS' /Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/commands/team-lead/execute.md</automated>
  </verify>
  <done>
    - .claude/commands/team-lead/execute.md exists and is not a stub (does not contain "[STUB")
    - File contains $ARGUMENTS in the body
    - File contains "inProgress" (status transition reference, per pipeline stage map)
    - File contains "forTeamLeadCheck" (fourth stage status, per lifecycle)
    - File contains "Retry / Skip / Abort" (failure gate prompt, per D-09)
    - File contains "stub — Phase 3" (stub receipt log format, per D-08)
    - File contains "Done ✓" (progress trail format, per D-10)
    - File contains "one at a time" or "ONE STAGE AT A TIME" (anti-race-condition instruction, per Pitfall 6)
    - File contains "task-state-guard" (PIPE-05 note confirming existing hook handles transition enforcement)
    - All five pipeline stages are named: Developer, CodeReview, QA, TeamLeadCheck, Done
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify PIPE-05 is satisfied by task-state-guard.js</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .claude/hooks/task-state-guard.js — confirm it exists and contains VALID_TRANSITIONS map covering all 6 lifecycle states
    - .claude/settings.json — confirm task-state-guard.js is registered under PreToolUse with Write|Edit matcher
    - .planning/REQUIREMENTS.md — PIPE-05 requirement text (line containing "PIPE-05")
  </read_first>
  <action>
    This task verifies that PIPE-05 is satisfied by the existing implementation (no new code needed).

    Run the following verification:
    1. Confirm .claude/hooks/task-state-guard.js exists and contains the VALID_TRANSITIONS object with all 6 status values: readyForDevelop, inProgress, inReview, inTesting, forTeamLeadCheck, done.
    2. Confirm .claude/settings.json contains a PreToolUse entry for task-state-guard.js with a Write|Edit matcher.
    3. Pipe an invalid transition payload to confirm the hook denies it:
       echo '{"tool_name":"Edit","tool_input":{"file_path":".planning/work/test/TASK-001.md","old_string":"status: readyForDevelop","new_string":"status: done"}}' | node .claude/hooks/task-state-guard.js
       Parse the stdout JSON and confirm permissionDecision is "deny".

    Update REQUIREMENTS.md: change the PIPE-05 line from "- [ ] **PIPE-05**:" to "- [x] **PIPE-05**:" to mark it complete (it was already implemented in Phase 1 as task-state-guard.js PreToolUse hook; ROADMAP SC-2 was corrected to PreToolUse in plan 01-05).

    Update ROADMAP.md status tracker for PIPE-05 if a tracker table exists: change Pending to Complete.
  </action>
  <verify>
    <automated>node -e "const s=require('./.claude/settings.json'); const pre=s.hooks.PreToolUse; const found=pre.some(e=>e.matcher&&e.matcher.includes('Write')&&e.hooks.some(h=>h.command.includes('task-state-guard'))); console.log(found?'PIPE-05-OK':'PIPE-05-MISSING')" 2>/dev/null || echo "check-settings-manually"</automated>
  </verify>
  <done>
    - .claude/hooks/task-state-guard.js exists with VALID_TRANSITIONS covering all 6 lifecycle states
    - .claude/settings.json has a PreToolUse entry matching Write|Edit that invokes task-state-guard.js
    - Piping an invalid transition payload to task-state-guard.js returns permissionDecision:deny in stdout JSON
    - REQUIREMENTS.md PIPE-05 line is marked [x] (complete)
    - PIPE-05 is confirmed satisfied with no new hook code written
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| $ARGUMENTS (TASK-ID) | User-supplied — must be normalized and validated against glob results before use |
| task file Edit calls | Every write to .planning/work/**/*.md is intercepted by task-state-guard.js PreToolUse hook |
| stub agent receipts | Phase 2 stubs are safe strings; Phase 3 real agents will replace them with actual invocations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | execute.md status transitions | mitigate | task-state-guard.js PreToolUse hook validates every Edit to .planning/work/**/*.md — invalid transitions are denied before reaching disk |
| T-02-10 | Tampering | TASK-ID path traversal via $ARGUMENTS | mitigate | Execute command body instructs Claude to normalize to TASK-NNN format and resolve via Glob — the resolved path is within .planning/work/ by construction |
| T-02-11 | Denial of Service | Infinite pipeline loop | mitigate | D-09 prohibits automatic retry — all failures require explicit user input (Retry/Skip/Abort); stop-guard.js (Plan 01) guards the Stop hook |
| T-02-12 | Tampering | repo:both injection via task file during pipeline | mitigate | task-state-guard.js validates repo field on every Write/Edit and denies repo:both — execute command body never writes repo field (only writes status) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; execute.md is pure markdown instructions — no supply-chain risk |
</threat_model>

<verification>
Manual verification (run after execution):
1. Run /team-lead:execute TASK-001 against a task file with status:readyForDevelop. Verify the progress trail appears stage by stage.
2. Confirm stub receipts are printed for each stage in format "[<Stage>] stub — Phase 3 plugs in real agent".
3. Confirm final status of the task file is "done" after all stages complete.
4. Test ID normalization: run /team-lead:execute 1 — verify it resolves to TASK-001.

Automated:
grep -c '\$ARGUMENTS' .claude/commands/team-lead/execute.md
grep -c 'Retry / Skip / Abort' .claude/commands/team-lead/execute.md
node -e "const s=require('./.claude/settings.json'); const pre=s.hooks.PreToolUse; const found=pre.some(e=>e.matcher&&e.matcher.includes('Write')&&e.hooks.some(h=>h.command.includes('task-state-guard'))); console.log(found?'PIPE-05-OK':'PIPE-05-MISSING')"
</verification>

<success_criteria>
- .claude/commands/team-lead/execute.md is no longer a stub — full pipeline orchestration logic present
- Covers TL-02: Developer → CodeReview → QA → TeamLeadCheck → Done stages all present with status transitions
- Covers D-08: stub receipts logged at each stage ("[Stage] stub — Phase 3 plugs in real agent")
- Covers D-09: failure prompts user with Retry / Skip / Abort (no auto-retry)
- Covers D-10: progress trail printed after each stage ("[Stage] Done ✓")
- Covers PIPE-05: confirmed complete via task-state-guard.js verification; REQUIREMENTS.md marked [x]
</success_criteria>

<output>
Create `.planning/phases/02-teamlead-skills/02-03-SUMMARY.md` when done
</output>
