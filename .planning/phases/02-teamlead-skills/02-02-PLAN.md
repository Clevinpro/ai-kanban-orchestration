---
phase: 02-teamlead-skills
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/team-lead/plan.md
autonomous: true
requirements: [TL-01, TL-03, TL-04]

must_haves:
  truths:
    - "Running /team-lead:plan path/to/SPEC.md validates all four required section headers before generating tasks"
    - "Running /team-lead:plan --new epic-name writes a SPEC.md template with all four section headers"
    - "Plan command prints a markdown review table (ID | Title | Complexity | Repo | Epic) before writing any files"
    - "Plan command asks 'Write these tasks? [y/N]' and only writes TASK-XXX.md files after user confirms with y/Y"
    - "Each generated TASK-XXX.md has all 9 required frontmatter fields with status:readyForDevelop and repo:be or repo:fe (never both)"
    - "Each generated task has a complexity score (integer 1-10) visible in the review table and in the task file frontmatter"
    - "Epic slug is derived from ## Goal section of SPEC.md (or filename slug fallback); tasks land in .planning/work/<slug>/"
  artifacts:
    - path: ".claude/commands/team-lead/plan.md"
      provides: "Full plan command implementation"
      contains: "$ARGUMENTS"
    - path: ".claude/commands/team-lead/plan.md"
      provides: "SPEC.md section header validation step"
      contains: "## Goal"
    - path: ".claude/commands/team-lead/plan.md"
      provides: "Review table + confirm gate"
      contains: "Write these tasks?"
  key_links:
    - from: "/team-lead:plan invocation"
      to: ".claude/commands/team-lead/plan.md"
      via: "Claude Code slash command dispatch"
      pattern: "\\$ARGUMENTS"
    - from: "plan.md body"
      to: ".planning/work/<slug>/TASK-XXX.md"
      via: "Write tool after user confirms"
      pattern: "\\.planning/work/"
    - from: "generated TASK-XXX.md"
      to: ".claude/hooks/task-state-guard.js"
      via: "PreToolUse Write intercept"
      pattern: "readyForDevelop"
---

<objective>
Replace the plan.md stub with a full implementation that turns a SPEC.md file into reviewed TASK-XXX.md files. The command validates SPEC.md structure, derives an epic slug, reads codebase context, reasons about task decomposition with complexity scoring, displays a review table, asks for confirmation, and writes files only after user approval.

Purpose: Gives the TeamLead the ability to break a SPEC.md into reviewed, schema-conformant task files — the entry point to the entire pipeline.

Output: `.claude/commands/team-lead/plan.md` (complete replacement of stub).
</objective>

<execution_context>
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-teamlead-skills/02-CONTEXT.md
@.planning/phases/02-teamlead-skills/02-RESEARCH.md
@.planning/phases/02-teamlead-skills/02-PATTERNS.md
</context>

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From .claude/commands/team-lead/plan.md (current stub — keep frontmatter, replace body):
  name: team-lead:plan
  description: Break a SPEC.md epic into TASK-XXX.md files with complexity scores. Pauses for human review before any code executes.
  argument-hint: "<path-to-SPEC.md>"
  allowed-tools: Read, Write, Bash, Glob, Grep

  UPDATE argument-hint to: "<path-to-SPEC.md> | --new <epic-name>"

From .planning/task-schema.yaml (required TASK-XXX.md frontmatter fields):
  id: TASK-NNN  (three-digit zero-padded)
  title: string
  status: readyForDevelop  (always on creation)
  priority: medium  (default; adjust per task)
  repo: be | fe  (never both)
  epic: <slug>  (must match parent directory name)
  complexity: 1-10  (integer)
  created-at: ISO8601
  updated-at: ISO8601

From .planning/work/test-epic/TASK-001.md (canonical example — check structure of description body):
  ## Description, ## Acceptance Criteria, ## Technical Notes sections follow the frontmatter

From .planning/codebase/ (files to read for task decomposition context):
  STRUCTURE.md, ARCHITECTURE.md
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write full plan.md instruction body</name>
  <files>.claude/commands/team-lead/plan.md</files>
  <read_first>
    - .claude/commands/team-lead/plan.md — read current stub to confirm frontmatter fields (keep them, update argument-hint)
    - .planning/task-schema.yaml — all 9 required frontmatter fields and allowed repo/status values
    - .planning/phases/02-teamlead-skills/02-PATTERNS.md — plan.md pattern section (argument dispatch, validation, slug derivation, review table, write-on-confirmation, auto-numbering guard, task frontmatter shape)
    - .planning/phases/02-teamlead-skills/02-RESEARCH.md — Pattern 3 (SPEC.md section validation), Pattern 4 (epic slug derivation), Pattern 5 (status transition), Anti-Patterns to Avoid, Code Examples (plan.md key sections)
    - .planning/work/test-epic/TASK-001.md — canonical TASK file example showing description body structure
  </read_first>
  <action>
    Replace the entire body of .claude/commands/team-lead/plan.md (everything after the closing ---).
    Keep all existing frontmatter fields. Update argument-hint to: "<path-to-SPEC.md> | --new <epic-name>".

    The body must implement these steps in order (prose instructions for Claude to follow):

    STEP 0 — Argument dispatch:
    Check $ARGUMENTS. If it starts with --new, treat the next word as <epic-name>. Write a SPEC.md template to ./<epic-name>/SPEC.md (or ask the user for a path if <epic-name> is ambiguous). The template must contain exactly these four section headers with placeholder content: ## Goal, ## User Stories / Requirements, ## Acceptance Criteria, ## Technical Design. Stop after writing — do not proceed to task generation.
    Otherwise, treat $ARGUMENTS as a path to an existing SPEC.md.

    STEP 1 — Validate SPEC.md:
    Read the file at $ARGUMENTS. Use Grep to verify all four section headers are present in the file:
    - ## Goal
    - ## User Stories / Requirements
    - ## Acceptance Criteria
    - ## Technical Design
    If any header is missing, output the list of missing headers (as a bulleted list) and stop. Do not proceed to task generation.

    STEP 2 — Derive epic slug:
    Read the text under ## Goal. Take the first sentence (or first 5-8 words), convert to lowercase kebab-case (remove punctuation, replace spaces with hyphens). Example: "Build a user authentication system" → user-authentication-system. Fallback: if ## Goal is empty or absent, use the SPEC.md filename without path or .md extension, converted to kebab-case. The derived slug is used as the directory name: .planning/work/<slug>/.

    STEP 3 — Read codebase context:
    Read .planning/codebase/STRUCTURE.md and .planning/codebase/ARCHITECTURE.md (if they exist). Use this to understand where new code would land and which sub-repo (be or fe) each task belongs to. This informs task granularity and repo assignment.

    STEP 4 — Check existing task IDs:
    Use Glob on .planning/work/<slug>/TASK-*.md to find all existing task files. Find the highest existing ID number. New task IDs start one above that maximum, zero-padded to three digits. If no tasks exist, start at TASK-001.

    STEP 5 — Generate task list:
    Reason about the SPEC.md content and derive a list of tasks. For each task, produce a TASK-XXX.md file content with:
    - Frontmatter: id (TASK-NNN), title (focused action title, ~10 min execution, one action per task), status (readyForDevelop), priority (medium by default; adjust to high for critical path items), repo (be or fe — NEVER both; split full-stack work into separate tasks), epic (<slug>), complexity (integer 1-10 where 1=trivial, 10=most complex), created-at (current ISO8601 timestamp), updated-at (same as created-at)
    - Body sections: ## Description (one paragraph), ## Acceptance Criteria (checklist), ## Technical Notes (implementation hints)
    Ordering: tasks in recommended execution order (flat list, no wave grouping).

    STEP 6 — Display review table:
    Print the following table BEFORE writing any files:
    | ID | Title | Complexity | Repo | Epic |
    |----|-------|------------|------|------|
    (one row per task)
    Then print: "Write these tasks? [y/N]"

    STEP 7 — Write on confirmation:
    If the user replies y or Y: write each TASK-XXX.md to .planning/work/<slug>/ using the Write tool. Print "Written: <filename>" for each file written.
    If the user declines (any other input): stop without writing any files. Print "Aborted. No files written."

    Constraints reminder (include as a ## Constraints section at the top of the body, BEFORE the steps):
    - Max task size: ~10 min execution time. If a task would take longer, split it.
    - Fresh context window per task. Each task must be fully self-contained.
    - Never set repo: both. Split full-stack work into separate be and fe tasks.
    - Tasks always created with status: readyForDevelop. The PreToolUse hook (task-state-guard.js) will reject any other initial status.
  </action>
  <verify>
    <automated>grep -c '\$ARGUMENTS' /Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/commands/team-lead/plan.md</automated>
    <automated>grep -c 'Write these tasks' /Users/tarasbannyi/TestAI/ai-agent-microservices/.claude/commands/team-lead/plan.md</automated>
  </verify>
  <done>
    - .claude/commands/team-lead/plan.md exists and is not a stub (does not contain "[STUB")
    - File contains $ARGUMENTS in the body
    - File contains "Write these tasks? [y/N]" (the confirmation gate, per D-05)
    - File contains "## Goal" (section header validation reference, per D-02)
    - File contains "readyForDevelop" (initial status constraint, per task schema)
    - File contains "repo: both" with a prohibition context (never emit repo:both, per task-state-guard.js)
    - File contains "complexity" (scoring instructions, per TL-04)
    - File contains ".planning/work/" (target write path, per D-04)
    - grep -c 'Step\|STEP\|## Step' .claude/commands/team-lead/plan.md returns at least 5 (covers STEP 1-7 compressed)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| $ARGUMENTS | User-supplied path to SPEC.md — could point to any readable file |
| SPEC.md content | Free-form prose — no execution; read-only input to Claude's reasoning |
| .planning/work/ writes | Claude writes TASK-XXX.md only after explicit y/Y confirmation from user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | Generated TASK-XXX.md frontmatter | mitigate | task-state-guard.js PreToolUse hook rejects any initial status other than readyForDevelop and rejects repo:both at write time — the plan command body also instructs Claude to never set these values |
| T-02-06 | Information Disclosure | $ARGUMENTS path | accept | $ARGUMENTS is user-supplied in their own local session; no untrusted user input crosses a trust boundary here |
| T-02-07 | Spoofing | Epic slug injection via SPEC.md ## Goal | mitigate | Plan command body instructs Claude to convert the slug to lowercase kebab-case, removing punctuation and special characters before using it as a directory name |
| T-02-08 | Tampering | SPEC.md path traversal | accept | Claude Code's file permission model governs which paths Claude can read; no additional restriction needed for a local developer tool |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; plan.md is pure markdown instructions — no supply-chain risk |
</threat_model>

<verification>
Manual verification (run after execution):
1. Create a minimal SPEC.md with all four required headers. Run /team-lead:plan against it. Verify the review table appears before any files are written.
2. Verify the review table columns are: ID | Title | Complexity | Repo | Epic (per D-06).
3. Reply y — verify TASK-XXX.md files are written to .planning/work/<slug>/ with all 9 required frontmatter fields.
4. Run /team-lead:plan against a SPEC.md missing ## Technical Design. Verify an error listing the missing header is output and no files are written.
5. Run /team-lead:plan --new test-epic. Verify a SPEC.md template is written with all four section headers.

Automated:
grep -c '\$ARGUMENTS' .claude/commands/team-lead/plan.md
grep -c 'Write these tasks' .claude/commands/team-lead/plan.md
</verification>

<success_criteria>
- .claude/commands/team-lead/plan.md is no longer a stub — contains a full multi-step instruction body
- Covers TL-01: reads SPEC.md + codebase context, generates tasks, outputs review list before execution
- Covers TL-03: validates four required section headers (## Goal, ## User Stories / Requirements, ## Acceptance Criteria, ## Technical Design)
- Covers TL-04: complexity score 1-10 assigned per task, visible in review table and frontmatter
- Human review gate (D-05): "Write these tasks? [y/N]" confirmation before any write
- Review table format (D-06): | ID | Title | Complexity | Repo | Epic |
- Template generation (D-03): --new flag writes starter SPEC.md
- Epic slug derivation (D-04): derived from ## Goal, falls back to filename slug
</success_criteria>

<output>
Create `.planning/phases/02-teamlead-skills/02-02-SUMMARY.md` when done
</output>
